# AMIVM上で言語を実装するときのヒント(Weaveの実装から)

> 取り込み元: Weave(`github.com/amisonnet8/weave`)の実装過程(前半8ステップ+後半5ステップ+モジュール機能+CLOSネスト全面移行+自己再帰+制御構文のブロック形IF/LOOP移行+ASSERTによる型ヒント+Go資産の戻り値のlist一般化+govar+スライス型ヒント+0引数関数糖衣構文+AMIVM本体へのMETHOD命令追加+スライス型ヒントの一般化+DEFERによるパニック捕捉+文字列操作/数学関数/exitの組み込み関数拡張)で得られた知見。
> 次にAMIVM上で別言語を実装するAI向けの申し送り。**`cascade_implementation_notes.md`の続編にあたるが、CascadeがSeedの推測を検証する話だったのに対し、Weaveは全く違う軸(動的型付け言語をAMIVM上に実装する)を歩いた。** Seed/Cascadeはどちらも静的型付け言語で、値の実行時表現をAMIVMのネイティブ型(`^int`/`^string`/`^*Struct`等)へ素直に落とし込めたが、**Weaveの値は全てGoの`any`**(数値は常に`float64`)であり、この前提の違いがSeed/Cascadeの知見のほとんどを効かなくする。Weave自身の言語仕様の話ではなく、Seed/Cascadeと同じく「AMIVM-IRを生成するフロントエンドを書くときに踏む地雷・使える型」の話に絞っている。

## 0. Weaveが実証した命令 — 全64命令中たった20命令

Weaveの言語仕様(動的型・プロトタイプOOP・カリー化・アクター・Go資産連携・モジュール)は、**驚くほど少ない命令だけ**で実装できた。

```
VAR SET
IF ELSE ENDIF
LOOP BREAK CONTINUE ENDLOOP
NOT
ASSERT
METHOD
FUNC ENDFUNC RET
CALL
CLOS ENDCLOS
SLTYPE
DEFER
```

これで全て。`GOTO`/`LABEL`は(amivmのブロック形`IF`/`LOOP`導入を受けて全面移行した後は)**一度も使っていない**——§11参照。`ELIF`も同様に未使用(理由も§11)。Cascadeが「全64命令を1つ残らず使った」のと対照的に、Weaveは**`GVAR`すら一度も使わなかった**(§5参照)。`$N`(関数引数)も同様——Weaveのトップレベル関数(`weave_main`)は引数を取らないため出番が無い。算術・比較・論理・文字列結合・ビット演算・ポインタ・構造体・map・チャネル・select・SPAWN・SLICEは、ネイティブ命令を一切試さずに**最初から**独自Goランタイム(`weavert`)への`CALL`一本化を選んだ(§1参照)。`NOT`だけは唯一のネイティブ論理演算命令として使っている(§11: ループの終了条件反転・`||`の短絡評価反転に、すでに`^bool`型が確定している値へ直接使う)。`ASSERT`は当初(§6)全く出番が無いと判断していたが、後日「呼び出し側が任意で型を宣言できる」という設計(§12)で初めて使われるようになった——**「動的型付け言語だから未実証」という判断は、あくまでその時点の設計の範囲内での話であり、設計そのものを見直せば前提が覆ることがある**、という教訓が§12の核心。`SLTYPE`も同じ枠に入るさらに後日の追加(§16)——ただし§2が「`MPTYPE`系のコンテナ命令は`^any`一本化と相性が悪いので最初から避けてよい」と結論づけた話の**単純な撤回ではない**点に注意。§16で使えたのは「実行時の値そのものをコンテナ型で表現する」という§2が退けた使い方ではなく、「型ヒントという静的情報が1つでも増えたら、コンパイラ自身がその1つの具体的な型のためだけに専用の名前付き型を1つ発行する」という、限定的かつ全く別の使い方だった。`DEFER`はさらに後日、パニック捕捉機能(§19)で初めて使われた——SPAWNとは異なり「ネイティブ命令を試さず最初からweavertへ」という判断すらせず、最初から`DEFER`一択だった、これも§19参照の特殊な事情による。

**`FNTYPE`/`FGET`は一度使われたが、後日Weave自身の設計変更で丸ごと使わなくなった、この表でユニークな存在——「実証したが最終的には不採用になった命令」の記録として残しておく。** §12でメソッド値取得のために採用したが、§17・§18で詳述する通り、名前付き代役型と組み合わせると関数型の同一性要求で壊れることが判明し、amivm本体に新設してもらった`METHOD`(`local := variable.method`、型推論)に完全に置き換えた。次にAMIVM上で言語を実装する人へ: **`FGET`(`single1 = variable.field`という既存VARへの代入)は、対象が「型ヒントを持つ動的言語のメソッド値」である場合は最初から避け、`METHOD`(存在すれば)を使うこと** ——理由の全体像は§17・§18を参照。

## 1. 動的型付け言語はAMIVMのほぼ全命令と相性が悪い(最重要の教訓)

AMIVMは「命令をほぼそのままGo構文へ1:1変換し、型検査は`go/types`に丸投げする」という設計で、これは**フロントエンド言語の変数がAMIVMに渡る時点でおおむね静的に型が定まっている**ことを暗黙の前提にしている。`MPTYPE`/`STTYPE`/`FNTYPE`/`CHTYPE`/`SLTYPE`のようなコンテナ系命令は、宣言した具体的なGo型を持つ変数を要求するが、Weaveの変数は宣言時から例外なく`^any`——**`^any`型の変数を対象に、mapインデックス・構造体フィールド・チャネル送受信を直接行う命令はAMIVMに存在しない**(型アサーションを自動で挟んでくれる命令が無いため)。

結果として、オブジェクト(§2)・クロージャー(§4)・アクター(§5)・Go資産呼び出し(§6)のいずれも、「まずネイティブ命令を試して壁にぶつかる」のではなく、**最初から独自Goランタイムへの一元化を軸に設計する**という判断に収束した。次に動的型付け言語をAMIVM上に実装するなら、この結論を最初から前提にしてよい——回り道の価値は無い。

**唯一の例外は`IF`**: `IF boolean1 label`はGoの`if boolean1 { goto label }`を生成するため、`boolean1`はGoのネイティブ`bool`型でなければならない。`any`型の変数(中身がboolでも)はそのまま条件式にできない、という「値は全て`^any`」原則の唯一の穴で、実装するまで気づかなかった落とし穴だった。対策は`weavert.CheckBool(v any) bool`という1回の`CALL`を経由するだけで済み、コストは低い。**`^any`一本化の中で、Go自身の制御構文が要求するネイティブ型と衝突するのは`IF`(条件式)だけ**、という所在を先に知っておくと迷わない。

## 2. オブジェクト・map:ネイティブ`MPTYPE`系は最初から候補から外してよい

Weaveのプロトタイプオブジェクト(`{x:1,y:2}`)はGoの`map[string]any`として実装したが、`MPTYPE ^string ^any`+`MGET`/`MSET`を使おうとすると、「`MPTYPE`宣言の具体的な型」と「Weave変数の宣言型(`^any`)」が食い違い、そのまま組み合わせられない(§1の帰結)。`weavert.Object`(素のGo `map[string]any`)+ランタイム関数(`ObjGet`/`ObjSet`/`ObjHas`/`ObjRemove`/`ObjKeys`)への完全な一元化が最終形になった。

**Cascadeで確立した`MPKEYS`(map走査ネイティブ命令)も、この設計の下では出番が無かった**——そもそも`MPTYPE`宣言自体を使わないため。`for-in`は`weavert.ObjKeys`+`weavert.Len`+`weavert.KeyAt`と、カウンタ付きの`IF`/`GOTO`/`LABEL`ループで実装した。**「前の言語で追加してもらった命令だから使えるはず」と決めつけず、自分の言語の値表現(`^any`一本化)と噛み合うかを毎回個別に検証すること。**

## 3. プロトタイプチェーン・メソッド呼び出し・アクター:1つのヘルパーに集約

`obj.__proto__`を辿る再帰的プロパティ検索は、AMIVM側のループ展開ではなく`weavert.ObjGet`という1つのGoランタイム関数の内部ループで完結させた。Weave言語設計の核(オブジェクトのプロパティ読み取り・プロトタイプ継承によるメソッド呼び出し・アクターへのメッセージ送信を「名前による検索」という1つの仕組みに統一する)は、コード生成レベルでも`ObjGet`+`Call`という共通ヘルパーを3箇所全てで共有することでそのまま実現できた。**「言語設計上の統一原理」が「実装上のヘルパー共有」に直結するのは、動的ディスパッチが最初から1つのランタイム関数に集約されているからこそ**——ネイティブ命令に分散させていたら、この共有は成立しなかった可能性が高い。

## 4. カリー化・クロージャー:`CLOS`のネスト制約と、その後の解消

初期実装ではAMIVMの`CLOS`が他の`CLOS`本体の中にネストできない制約があり(Seedの申し送り通り)、Weaveのカリー化(`fn(a) fn(b) {...}`、クロージャーを返すクロージャー)を素直に表現できなかった。当初の回避策は「各クロージャーリテラルを独立したトップレベル`FUNC`へコンパイルし、自由変数を明示的な環境スライスとして渡す」という手動クロージャー変換(`SLMAKE`+`ASET`で環境を構築、`reflect.Value.Call`で呼び出す)だった。この方式は動くが、副作用として**捕捉が値渡し(スナップショット)になり、Goの本来のクロージャーが持つ参照捕捉と異なる意味論になった**(呼び出しをまたいだ状態の蓄積ができない、自己再帰ができない、という制約が生まれた——ドキュメント化されるまで気づかれていなかった)。

**後日、AMIVM本体に`CLOS`のネスト許可+クロージャー引数の階層アドレッシング(`&N`=自分のいる階層のN番目、`&L-N`=階層`L`を明示指定、Go側の実名にも階層`L`を`amivm_closureL_paramN`として埋め込む)が追加され、ネイティブなクロージャーの入れ子が可能になった。** 対応の要点:

- `&L-N`の`L`は`FUNC`直下を1として数える**絶対的な深さ**であり、「相対的に何段上か」ではない。フロントエンド側は自分がいま生成しているCLOSの絶対的な深さを追跡するだけでよく、相対的な計算は一切不要
- **`&L-N`だけではネストしたCLOS間の`%name`(通常のローカル変数)の衝突は解決されない。** `%xxx`のGo実名は「外側の`FUNC`の名前で修飾される」規則のままで、ネストの深さに応じて変わらない——複数のCLOSが同じ`%tmp`のような名前を使うと、内側が外側をシャドーイングしてしまう。**この一意化はamivm側の改修範囲外であり、フロントエンド自身が担う必要がある。** Weaveでは各CLOSインスタンスに一意な接頭辞(`c7_`のような)を割り当て、コード生成側に「自分自身への新規宣言」と「親を辿って既存の束縛を探す」という2種類のAPIを持つ真のスコープチェーン(`parent`リンク付きの構造体)を新設して解決した——**「codegen側はスコープを持たず、semaが保証した正しさをそのまま信頼してよい」という単一`FUNC`内で完結する前提(Seed/Cascade由来)は、ネストした`CLOS`が実在するようになった時点で崩れる**、という教訓
- ネイティブな`CLOS`へ移行すると、自由変数の明示的な収集・環境スライスの構築が完全に不要になった(Goの`func`リテラルが自然にレキシカルキャプチャしてくれるため)。同時に捕捉が値渡しから参照捕捉に切り替わり、複数呼び出しをまたぐ状態蓄積・自己再帰(名前へ直接代入する形の`fact = fn(n) {...fact(n-1)...}`限定)が新たに実現できるようになった
- クロージャー値そのものは(ネイティブ`CLOS`でも)`^any`型変数に格納されるため、**呼び出し自体は依然としてreflect経由が必要**(`IF`が`^bool`を要求するのと同種の壁)。「ネストと捕捉がネイティブに書けるようになった」ことと「呼び出しがネイティブになる」ことは別問題、という区別が重要

## 5. アクター:チャネル系命令は最初から検討すら省略してよい

動的型付け言語でオブジェクト・クロージャーが軒並みAMIVMネイティブ命令と相性が悪いと分かった段階で、アクターの受信ループも「実装してから壁にぶつかる」前に**最初からGoネイティブのgoroutine/channelをランタイム内に隠蔽する設計**にした(`CHTYPE`/`CHMAKE`/`CHSEND`/`CHRECV`/`SPAWN`は一度も試していない)。受信ループのディスパッチ自体が§3の動的プロパティ検索を必要とする以上、チャネルの配線だけをネイティブ命令にしても得るものが無いという判断。**「同種の制約に何度も当たった後は、次の機能で同じ壁を予測して先回りする」**という判断パターンが、Weaveでは実装してから気づくよりも先に効いた。

## 6. Go資産連携:静的解決名の実体はやはりreflect(§12で一部覆る)

`gotype`/`gofunc`/`gomethod`(コンパイル時にのみ意味を持ち、実行時のVAR/SETを一切生成しない宣言)によって、Go資産の呼び出しは**名前解決だけ**は静的に確定できる(動的なプロトタイプ検索を経由しない)。しかし**呼び出し自体は依然としてreflect経由になる**——Weave変数は常に`^any`で、生成されたGoコードは実際のGo型(`*strings.Reader`等)を静的に知らないため、ネイティブなGo呼び出し構文を直接出力できない。「静的解決」の実質的な意味は「名前がIRに直接埋め込まれ、動的なプロトタイプチェーン探索を経由しない」という点にとどまる。

**reflectで包んだGo資産呼び出しが多値を返す場合、`(値, error)`というGo最頻出のパターンだけは、戻り値の「個数」ではなく「型」で判定すると綺麗に扱える。** `reflect.Type.Implements(reflect.TypeOf((*error)(nil)).Elem())`で最後の戻り値の型が`error`インタフェースを満たすかを見て、満たしていて実際に`nil`でなければ言語側のpanicに変換し、満たしていなければ(またはerrorがnilなら)先頭の値だけを1値言語の戻り値として使う。個数だけで判定すると、`(int, int)`のような非errorの多値を誤ってerror扱いしてしまう——**型で判定するからこそ、この誤判定を避けられる**。動的型付け言語でreflectベースのGo資産連携を作る場合、この一手間は入れておく価値が高い(標準ライブラリの大半の多値関数がこの形のため)。あわせて、対象言語が「バイト列」という概念を持たない場合(Weaveは持たない)、`[]byte`をその言語の文字列型へ寄せる変換も、数値正規化と同じ発想で足しておくと実用性が上がる(`os.ReadFile`のような頻出関数がまさにこの形で返す)。

**この結論は「型を宣言しない場合」の話であり、後日(§12)「型を任意で宣言できる」という設計を追加したことで、宣言した経路に限りreflectを完全に排除できることが分かった。** `^any`という壁そのものは変わらないが、`ASSERT`(Goの型アサーション`v.(T)`)を使えば、呼び出しの直前に`any`から具体型へ実行時に降ろせる——「静的に確定できるのは名前だけ、呼び出しは常にreflect」という結論は、ASSERTという命令の存在を知らなかった(あるいは動機が無かった)段階での結論だったに過ぎない。

- `value`オペランドカテゴリには`!xxx`(AMIVM関数)だけでなく`?xxx`/`?xxx.yyy`(Go関数そのもの)も含まれる——**関数値そのものを、呼び出し対象(callname)としてではなく普通の引数値として、別の`CALL`へ渡せる。** この事実は仕様書を読んだだけでは見落としやすい(callnameカテゴリの`?xxx`と混同しがち)。Go関数を「文字列名から動的に解決」しようとせず、「関数値そのものを値として渡してreflectで呼ぶ」設計に切り替えるだけで、リテラル引数以外(変数・オブジェクトプロパティ)を渡すケースでも壊れない、より一般的な仕組みになる
- 引数がリテラルの場合だけ、Goのネイティブな`CALL result : ?pkg.Func arg`という直接呼び出しが偶然成立する(無型定数がどんな具体的パラメータ型にも暗黙変換できるため)。これは**動的型変数(`^any`)を渡した瞬間に`go/types`エラーで崩れる、脆い成功**であり、最初からreflect経由で設計する方が安全(実地検証で「今まで動いていたのはリテラルしか渡していなかったから」と発覚した)

## 7. 自由変数解析・自己参照束縛で踏んだ実装バグ2件

- クロージャー本体の中で、コンパイル時専用の名前(`gofunc`宣言済みの名前など、実行時のVAR/SETを一切持たない識別子)を通常の識別子と同列に扱うと、「外側スコープから捕捉しようとして未定義変数を参照する」バグになる。ビルトイン名の除外リストと同じ発想で、こうした「実際には変数として存在しない特別な呼び出し対象」も併せて除外する必要がある
- **「宣言してトークンを返す」ヘルパーの返り値を、呼び出し側が握りつぶして装飾前の名前をそのまま使ってしまうバグ**は、ネスト・接頭辞付けが導入されて初めて顕在化する(接頭辞が空文字列のトップレベルでは偶然一致するため気づけない)。シグネチャを「宣言する副作用のみ」から「宣言した実際のトークンを返す」へ変更したら、既存の全呼び出し元を機械的に洗い出すだけでなく、**返り値を実際に使っているかまで確認する**こと。ビルドエラーにならないぶん見逃しやすい

## 8. `GVAR`が丸ごと不要になった理由

動的型付け言語でも構造上GVARが要らなくなることがある、という一例。CascadeはGVAR(パッケージレベル変数)が複数のトップレベル関数(struct/レシーバー関数等)間の状態共有に必要だったが、**Weaveにはトップレベル関数が`main`(`weave_main`)しか存在しない**——他の全ての「関数」は`fn(a) {...}`クロージャーであり、値経由でしか到達できず、トップレベルで名前束縛されることもない。トップレベル文(`gotype`/`gofunc`宣言・プロトタイプオブジェクト定義)も、複数ファイル・複数パッケージ(モジュール機能)による束縛も、最終的には全て同じ`weave_main`の同じ`funcGen`へ流し込むだけで済み、GVARの出番が一度も無かった。**「フロントエンド言語がトップレベル関数を複数持つかどうか」がGVAR要否の分水嶺になる**、という判断基準を得た。

## 9. 開発プロセス上の教訓

- Seed/Cascade同様、「機能単位の縦切り+各ステップでamivm→go buildの実地検証必須」が変わらず最重要。Weaveでも「ロジック上正しそうに見えても実地検証なしでは信用しない」で複数のバグを発見した(§7のバグ2件、§1の`IF`/`^bool`の罠など)
- **既存のサンプルプログラムを、新機能を追加するたびに横断的に再実行することの重要性を、Weaveでも独立に再確認した。** ある構文糖衣(メソッド呼び出しの`self`自動注入)を後から追加した際、それ以前に書いた別のサンプル(単なるプロパティ関数呼び出しのつもりだったコード)の意味が無条件に変わってしまい、何ステップも気づかれずに壊れたまま残っていた(Cascade§9の教訓と同種だが、Weaveでは「文法の意味そのものが後から変わる」という、より発見しにくい形で再発した)
- **動的型付け言語ならではの教訓**: 意味検証(sema)の責務がSeed/Cascadeより狭くなる。値の型に依存するエラー(数値+文字列の演算など)の多くはamivmの`go/types`にも頼れず(全て`any`なので型エラーにならない)、実行時にフロントエンド自身のランタイムが検出するしかない。「semaが全てのミスを防波堤として捕まえる」という前提が成り立たない場合、**ランタイム層に型アサーション+panicという最も素朴な検出方式を敷き、詳細なエラーメッセージの改善は後回しにする**、という割り切りが有効だった
- **全ての言語機能がAMIVM-IRに触れるとは限らない。** 複数行コメント(`/* ... */`)の追加は、字句解析層(トークン化の一歩手前で読み飛ばすだけ)で完結し、パーサー・AST・sema・codegenのいずれも変更不要だった——このプロジェクトで唯一、IR生成に一切触れなかった機能追加。ランタイムに対応する実行時表現を持たない、純粋に字句・構文レベルの機能(コメント、空白の扱い等)を追加する際は、真っ先に「そもそもIRまで届く必要があるか」を確認する価値がある——毎回ランタイム層・IR生成層まで手を広げようとすると、この種の機能では無駄な設計コストになる
- **新しいASTノード種別を追加する際は、codegen/semaだけでなく、ASTを独立に横断走査する全ての場所を`grep`で洗い出すこと。** Weaveで`list[index]`添字構文(新しい`*ast.IndexExpr`/`*ast.IndexAssignStmt`)を追加した際、codegen(IR生成)・sema(意味検査)には新ノードのハンドラを追加したが、**モジュール機構がAST全体を独自に(sema/codegenより前の段階で)走査してパッケージ修飾子を解決する専用パス**を見落とし、そのパスが持つ網羅的な`switch`が新ノードに対して「unsupported expression」エラーを返した——単一ファイルの実地検証だけでは踏まず、モジュール機能を使うテストケースで初めて発覚した。「新しいASTノードが増えたら、既存の同系統ノード(`PropExpr`のような)を`case`に持つ全てのファイルを機械的に洗い出す」というチェックリストに、**フロントエンドの主要パイプライン(lexer→parser→sema→codegen)以外にAST全体を走査する補助パスが無いか**を必ず含めること——モジュール解決・最適化パス・リンティングツールなど、後から追加された横断的な仕組みほど見落としやすい
- **AMIVM本体への機能要求は「回避策の実装コストが突出して高いか」で判断する**(Cascade§8の3段階基準がそのまま使える)。Weaveで実際にIR修正を推奨したのは`CLOS`のネスト不可(独自のreflectベースクロージャーランタイムを丸ごと構築する必要があった)の1件のみ——`IF`/`^bool`の制約・`SLMAKE`の`deftype`制約・`MPTYPE`/`MGET`が`^any`変数に使えない制約は、いずれも「窮屈だが回避策が軽量」と判定し、IR側の変更を求めなかった
- **「1つの choke pointに正規化ロジックを集約する」設計は、その choke pointを経由しない書き込み経路が本当に無いかを全て洗い出して初めて安全になる。** Weaveの`list`が文字列辞書順ソートと数値順を一致させるために、キーを内部的に固定桁数へゼロ埋めする変更を入れた際、`weavert`パッケージの公開関数(`ObjSet`)経由の書き込み(IR生成コードは全てこれを通る)は正規化の恩恵を自動的に受けたが、**同じパッケージ内にある、Goコードから`Object`型の生マップへ直接書き込む内部ヘルパー(`Args()`・Go資産呼び出しの戻り値を組み立てる`newList`)がこの choke pointを経由していなかった**ため見落とし、実地検証(既存サンプルの再実行)で初めて発覚した——読み取り側(`at(...)`、正規化されたキーで検索)と書き込み側(未正規化のキーで格納)が食い違い、存在するはずの要素が見つからないというパニックになった。新しい正規化ロジックをランタイム層に追加する際は、「その正規化が期待される値を生成している場所」を`grep`で全て洗い出し(型定義・リテラル構築パターンで検索するとよい)、**codegenが生成するIRから呼ばれる公開APIだけでなく、同じランタイム内でその値を直接組み立てている他の内部コードも含めて**、1つ残らず同じ choke pointを通しているか確認すること

## 10. 実務メモ

- `amivm -i name=path`は未使用でも自動的に消えるという申し送りは変わらず有効。Weaveは`weavert`用の1つのマッピングを全IRファイルに使い回した
- 独自のGoランタイム(`weavert`)は`go:embed`で自分自身のソースを埋め込み、コンパイラのCLIがビルドのたびにスクラッチ用の使い捨てGoモジュールディレクトリへコピーして「同一モジュール内のサブパッケージ」として解決させる、というSeed/Cascadeと同じ配布方式をそのまま踏襲した。動的型付け言語では**この方式が「一時しのぎ」ではなく最初から本命になる**(§1参照)——Cascadeの`cascadert`のように、後日ネイティブ命令の追加で丸ごと削除できる望みは薄い
- 複数ファイル・複数パッケージ(モジュール機能)を実装する際、Cascadeの`修飾子.識別子`はレシーバー呼び出し構文にたまたま意味的に無害に乗せられたが、**Weaveの`obj.method(args)`は既に「第1引数に`obj`自身を自動注入する」という別の意味論を持つ**ため、モジュールの`修飾子.名前`をそのまま同じ構文に乗せると誤動作する。対策は、パッケージ解決フェーズ(sema/codegenより前)で`修飾子.名前`をASTレベルでプレーンな識別子へ直接書き換えてしまうことで、この糖衣構文自体を素通りさせた。**既存の構文糖衣と新機能の構文が同じ記法を共有する場合、糖衣が発火する条件(ここでは「Calleeが直接PropExprかどうか」)を先に確認し、新機能側がその条件に誤って合致しないよう設計段階で回避すること**

## 11. amivmの破壊的変更(単一行`IF boolean1 label`→ブロック形`IF`/`LOOP`)への追従

amivm本体が、旧仕様の単一行`IF boolean1 label`(`if boolean1 { goto label }`という条件付き`goto`)を廃止し、`IF`/`ELIF`/`ELSE`/`ENDIF`(ブロック形if)・`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`(ブロック形ループ)へ置き換えた(後方互換無し)。Weaveの制御構文(if/elif/else・while・for-in・&&/||短絡評価)は全てこの廃止された旧命令の上に組んであったため、**Seed・Cascadeがどちらも先に経験済みの、3言語目としての追従作業**になった。両者のnotesを読んでから着手できたのは、このプロジェクトを通じて初めての「他言語の移行知見を設計段階から使えたケース」だった(前半・後半では毎回「実装してからamivmのエラーで気づく」パターンだったのと対照的)。

**Seed/Cascade共通の教訓がそのまま使えた点:**

- `ELIF`命令は使わず、`ELSE`の中に次の`IF`をネストする自前の展開にする。`ELIF`のオペランドは既に計算済みの値でなければならず、Weave/Seed/Cascadeいずれの言語も条件の計算に複数命令(Weaveなら`weavert.CheckBool`のCALL)を要するため、素直に`ELIF`へ渡せない。ネストなら各条件が「それより前が全て偽だった場合にのみ」計算される、遅延評価が自動的に保たれる(amivm自身のパーサが`ELIF`をこう内部展開しているのと同じ形)
- `while`相当は`LOOP`+条件反転+`IF{BREAK}ENDIF`という前置チェックにする(amivm自身のドキュメントが例示する変換そのもの)。条件の再チェックがループ**先頭**に来るため、ネイティブな`CONTINUE`だけで「条件再チェックして継続」が成立し、`LABEL`/`GOTO`が丸ごと不要になる
- `break`/`continue`は「最も内側のループを対象にする」という各言語共通の意味論が、Goの`break`/`continue`の意味論とそのまま一致するため、対象ループを追跡するスタック(Seed: `loopInfo`、Cascade: `breakTarget`/`continueTarget`)自体は今なお必要——ただし中身は「空ならネイティブBREAK/CONTINUE、ラベル付きならGOTO」という切替式になる
- VARの巻き上げ(全`VAR`宣言を`FUNC`/`CLOS`本体の先頭にまとめる仕組み)は、**理由が変わっても仕組み自体は維持するのが3言語共通の結論**。旧来の理由(「gotoが変数宣言を飛び越えられない」というGoの制約回避)はブロック形IF/LOOPの導入で消滅したが、巻き上げを前提にしたスコープ/シャドーイングの仕組み(Weaveなら§10「代入は既存を再代入優先」を実現する`funcGen`のフラットな`declared`集合)が別の形で巻き上げに依存しているため、外すコストの方が高いと3言語とも判断した
- **「ループ末尾に必須の後処理(インデックス前進など)があり、それが`continue`でスキップされると壊れる」というパターンは要注意——GOTO/LABELに頼るその場しのぎの解決策は「本体に`continue`が一度も無いとラベルが未参照でgo/typesエラー」という同じバグを毎回誘発する。** Seed(for-in)・Cascade(switchのbreak)がそれぞれ独立にこのバグを踏んでいる

**Weave独自の改善(Seed/Cascadeのどちらにも無かったパターン):** `for-in`のインデックス前進を、ループ**末尾**ではなく**先頭**(境界チェックの直前)に置く構成に変えることで、上記の「continueLabel」問題自体を発生させなかった。`idx`を`-1`から始め、`LOOP`に入った瞬間に真っ先に`idx = idx + 1`してから境界チェックする——これなら`continue`でループ先頭に戻っても必ずインデックス前進を通過するため、専用の継続ラベルが最初から要らない。Seed/Cascadeのnotesを先読みしていたおかげで「両者が独立に同じ罠を踏んでいる」という事実に気づけ、それを踏まえてWeave側だけ設計段階で回避できた。**「ループ本体の後に必須の後処理がある」という形自体を避け、「毎周回の先頭で済ませてから本題に入る」という形に倒せないか、before-not-after で組めないかを先に検討する**、という一般化した教訓として持ち越れる。

**Seed/Cascadeのnotesがノーヒントだった点:** `&&`/`||`の真の短絡評価。両言語とも短絡評価が必要な場面がなく(あるいは要求仕様に含まれず)、amivmネイティブの`AND`/`OR`(両オペランドを無条件に評価してからGoの`&&`/`||`へ委譲するだけ、短絡しない)をそのまま使っていた。Weaveは§8で真の短絡評価が必要だったため、`elif`のネストトリックを式レベルに応用する(`IF <条件> { <右辺を評価してCheckBool> } ENDIF`、結果は左辺の一時変数を使い回す)独自の設計が必要だった——**参考実装のnotesが全てをカバーしているとは限らない。自分の言語が要求する性質(ここでは真の短絡評価)を先に明確にし、参考実装がそれを持っていたかどうかを確認してから、無ければ自分で設計すること**。

**`ASSERT`(型アサーション)は当初(この移行の時点まで)3言語とも未使用だった——ただしWeaveは後日(§12)これを覆した。** `any`相当の動的型を持たない言語(Seed/Cascade)では出番が無いのは当然として、`any`一本化のWeaveでも、演算子・比較・プロパティアクセスを全て`weavert`のランタイム関数(`any`のまま動く)に一元化する設計を貫いていたため、この時点では`ASSERT`で`any`から具体的なGo型を取り出す動機が無かった。**「動機が無い」は「使えない」とは違う**、というのがこの後の展開が示す教訓——詳細は§12。

## 12. `ASSERT`は「動機さえあれば」動的型付け言語でも本当に使える——安全性のための任意型ヒントという動機

制御構文のブロック形移行のすぐ後、ユーザーから「`ASSERT`は本当に使い道が無いのか」と問われたのをきっかけに再検討し、**Weaveの設計に「呼び出し側が任意で型を宣言できる」という新しい機能(Go資産境界の`gotype`/`gomethod`/`gofunc`の型ヒント、および自前オブジェクトの`shape`/`checkShape`)を追加したところ、`ASSERT`/`FNTYPE`/`FGET`が初めて実際に使われるようになった**。§0〜§11までの「動的型付け言語では`ASSERT`の出番が無い」という結論は誤りではなかったが、**それは「言語の値が終始`any`のまま」という設計を前提にした結論であり、設計自体を変える(『型が分かっている場合だけ』という限定的な静的情報を追加する)と前提が崩れる**、という教訓が最大の収穫だった。

**目的は速度ではなく安全性——最初この点を見誤った。** 検討の初期段階では「`ASSERT`でreflectを避ければ速くなるはず」という前提で始めたが、実際に既存のreflectベース呼び出し(`target any`を受け取る関数)のシグネチャを見返すと、呼び出し側だけ`ASSERT`しておいても関数境界を越える時点で`any`に戻ってしまい、reflect自体は消えないと判明した。真に消すには「メソッドのシグネチャ自体をAMIVMの`FNTYPE`として宣言し、呼び出しを`FGET`+ネイティブ`CALL`に置き換える」という、もっと大掛かりな変更が要る。ここでユーザーから目的を「速度」ではなく「安全性(TypeScriptに対するJavaScript、Pythonの型ヒントと同じ——間違った型を渡した時に、reflectの生の失敗ではなく分かりやすいエラーで即座に落ちる)」に転換するよう指摘され、そこから設計が定まった。**「なぜASSERTを使いたいのか」という動機を最初にはっきりさせないと、実装の形も変わってくる**、という教訓。

**「オブジェクト全体の型」と「プロパティ1つ1つの値の型」を混同しないこと。** Weaveの自前オブジェクトはGo側では単一の名前付き型(`weavert.Object = map[string]any`)にしかならないため、コンテナ自体を`ASSERT`しても常に成功する自明な検査にしかならない——ここだけを見て「動的型付け言語の自前オブジェクトには`ASSERT`は効かない」と誤って結論づけてしまった(実際に一度この誤りを犯した)。しかし各プロパティの**値**(`any`から取り出した1つ1つ)は、実際には数値・文字列・別のオブジェクト等の具体的な型を持っており、ここへの`ASSERT`はGo資産境界への型ヒントと全く同じ形で成立する。**「コンテナは単一の動的型に集約されている」ことと「コンテナの中身1つ1つも同じ制約を受ける」ことは別問題であり、混同すると本来使える場面を見逃す。**

**名前付き型と無名型の区別が、唯一`ASSERT`の適用外になるケースを生んだ。** Weaveのクロージャーはネイティブ`CLOS`が生成する**無名の**`func(any) any`型のまま(事前に`FNTYPE`で名前付き型を宣言していない)。Goの型アサーションは「インタフェースの動的型が厳密に一致するか」を見るため、無名の関数型を持つ値を名前付き型(`^WeaveFn`のような)へ`ASSERT`しようとしても常に失敗する——シグネチャの形が一致していても、名前付き型と無名型は別物として扱われる。これを直すには全クロージャーの生成方式(`CLOS`の型宣言)自体を変える必要があり、1つの検証機能のためだけに持ち出す変更としては影響範囲が大きすぎると判断し、この1ケースだけ`reflect.Kind()`(値を実際には呼び出さない、コストの低い判定)で代替した。**「その言語の値のうち、無名のGo型として実装されているものだけは`ASSERT`が構造的に効かない」という限界を、動的型付け言語の設計時に把握しておくとよい。** クロージャー・関数値を無名関数型のまま実装している言語(Seedのような静的型付け言語でも、関数値をどう表現するか次第では同じ制約に当たりうる)は特に注意。

**Weave側の実装ミス(amivm側の設計不備ではない): `ASSERT`の`variable`オペランドはリテラルを受け付けない。** `ASSERT %concrete %ok "文字列リテラル" ^string`のように、対象の値がWeaveの文字列リテラルそのものだった場合、amivmが構文エラーを返した——`ASSERT`の第3オペランド(`variable`カテゴリ)は`$N`/`&N`/`%xxx`/`@xxx`/`@xxx.yyy`のみを許し、他の多くのオペランドカテゴリと違ってリテラルを含まない。当初これを「amivm側の狭すぎる制限」と捉えていたが、**Goの`x.(T)`構文自体が`x`にインタフェース型の式を要求し、型無し定数であるリテラルはそもそもインタフェース型の式になれない**(`"hello".(string)`は素のGoとしても書けない)と気づき、`variable`限定はGoの言語仕様を正確に反映した意図的な設計だと判断を改めた。仮に`value`(リテラルを含む、最も緩いカテゴリ)まで緩めたとしても、amivm自身が内部でリテラルを一時変数へ代入してからアサーションする、という全く同じ変換を代わりに行う必要があり、フロントエンド側の負担が減る以上のことは起きない——**IR修正を求めるほどの問題ではなく(分類3: 窮屈だが回避策が軽量)、正しくは「Weaveが誤った前提でコードを書いた」という自分自身の実装ミス**だった。対策は、対象の値がまだ変数参照(`%`/`@`/`$`/`&`で始まる)でなければ、まず`^any`の一時変数へ`SET`してからASSERTするという、1段クッションを挟むだけで済んだ。

**成果は「安全性」の実感を伴う形で確認できた:** 型が合わない値を渡すと、reflectの生のpanic(`reflect: Call using string as type int`のような、Weaveのユーザーには意味の分からないメッセージ)ではなく、`weave: argument 1 to ?strings.NewReader: expected ^string, got float64`という、どこで・何を期待して・実際は何だったかが一目で分かるメッセージで即座に停止するようになった。エラーメッセージの整形(`%T`のようなGoのformat verbを使う部分)だけは、ネイティブ命令だけでは原理的に実現できないため`weavert`の小さなヘルパーに残したが、**型の判定自体は完全にネイティブになった**——「型が分かっている場合はASSERT+ネイティブ、わからない場合(またはエラー整形のような原理的にネイティブ化できない仕事)だけweavert」という判断軸は、このプロジェクトを通じて一貫して有効だった。

**後日追加: `CALL`のネイティブな複数代入が、`(値, error)`という動的判定問題そのものを型ヒント経路では消し去った。** §6で「`(値, error)`は戻り値の型で判定するしかない」と結論づけたのはreflect経由の話であり、型ヒント(`goError(型)`)を書いた場合はそもそも判定が要らない——AMIVMの`CALL multi1 multi2 ... : callname ...`が元から複数の代入先を持てる(`amivm_spec.md`のオペランドカテゴリで`ASSERT`の`multi1 (multi2)`と全く同じ形)ため、「値」と「error」をそれぞれ別々の具体型を持つネイティブ変数として受け取り、`error`側をネイティブな`!= nil`比較で判定するだけで済む。reflectで`reflect.Type.Implements(errorType)`のような実行時の型検査をしていたのは、**「戻り値の型が静的にわからない」という前提があったから**であり、型ヒントでその前提そのものが崩れた以上、同じ問題を同じ方法で解く必要は無かった——「型が分かっている場面では、動的判定のロジックそのものが要らなくなることがある」という、単なる高速化を超えた教訓。ただし`[]byte`のようなスライス型は`type1`オペランドカテゴリに直接のリテラル形が無い(名前付き`SLTYPE`を経由しない限り`^[]byte`のようなトークンを型ヒントとして書けない)ため、型ヒント付き経路はまだ全てのGo資産に使えるわけではなく、reflect経由の経路と共存させる設計にした。

## 13. 「2値専用」の特別扱いをやめて「N値」の一般形に倒したら、コード生成がむしろ単純になった

CLAUDE.mdに記録した通り、Weaveの利用者がこのコンテナ内に限られることを踏まえ、Go資産の戻り値表現を「戻り値1個ならスカラー、`(値, error)`だけ特別扱い」から「常にlist」へ全面的に作り直した。AMIVM-IR生成の観点から見ると、これは**「N=2専用の特別ケース」として書いていたコードを、「任意のNに対応する一般形」へ置き換えただけ**だった、という点が最大の収穫だった。

- §12で確立した`goError(...)`の2値`CALL`(`CALL raw errVar : callname ...`)は、実装としては「target1、target2の2つを決め打ちでフォーマットする」コードだった。これを「targetsという可変長スライスをタブ区切りで並べる」ヘルパー(0個なら`CALL\t:\tcallname`、N個なら`CALL\ttarget1\t...\ttargetN\t:\tcallname`)に一般化しただけで、0個・1個・2個・それ以上のいずれも同じコードパスで扱えるようになった——**「まず2値専用のコードを書き、後でN値に一般化する」のではなく、一般化した方がむしろ元のコードより短く単純になった**、という発見はこのプロジェクトを通じて何度か繰り返されているパターン(§11の`for-in`のインデックス前進の位置替えなど)の再演と言える
- 各戻り値を`^any`へ箱詰めする処理も、既存の`list(...)`リテラル用ヘルパー(`weavert.NewObject`+`weavert.ObjSet`をループで回すだけ)をそのまま再利用できた——Go資産の戻り値専用の別ルートを作る必要が無かった。「新機能のための新しいランタイム関数」を先に検討する前に、「既存の別機能が使っている汎用の組み立てパターンを流用できないか」を先に確認する価値がある、という教訓
- 副次的な効果として、reflect経由の未型付き経路(`CallGoFuncList`/`CallGoMethodList`)からも、旧`panicIfTrailingError`(戻り値の最後の型が`error`インタフェースを満たすかをreflectで判定するロジック)が丸ごと消えた。「個数によって挙動を変える」という条件分岐自体をやめたので、判定するものが無くなった——**特別扱いを消すことは、その特別扱いを実装していたコードも一緒に消せる**という、当たり前だが見落としやすい効果

この経験から得られる一般的な教訓: **IR生成コードに「N=2の場合だけ複数代入を使う」のような特定の個数に依存した分岐が出てきたら、まず「任意のNに一般化するとどう書けるか」を検討する価値がある。** 多くの場合、一般化した方のコードは分岐が減って単純になる——「特定のケースを先に頑張って実装してから、後で一般化を検討する」のではなく、「一般形を先に設計してから、必要なら特定ケースへの最適化を後から足す」という順番の方が、少なくともAMIVMのような「複数代入・複数引数をネイティブに扱える」命令セットの上では割に合うことが多い。

## 14. 予約名パターンマッチと衝突しうる構文糖衣は、パイプラインの最も遅い地点に置く

Weaveは新しい構文キーワードを増やさず、`名前 = 予約された関数呼び出し(...)`という形(`gotype`/`gofunc`/`package(...)`等)をパーサーの外側(sema/codegen)でパターンマッチして特別扱いする、という設計を一貫して使ってきた。これ自体はAMIVM-IR生成の話ではないが、**この設計を採る言語に、後から「引数0個の呼び出し`f()`は`f(nil)`として扱う」という構文糖衣を追加しようとして踏んだ罠**は、IR生成フロントエンド全般に応用できる教訓を残した。

`f()`→`f(nil)`をパーサーの時点で無条件に展開すると、`goReturns()`(戻り値0個)・`goParams()`(引数0個)・`list()`(空リスト)のように、**それ自体が既にゼロ引数という形で意味を持つ、予約された呼び出し**の引数リストへ勝手に`nil`が混入し、それらの呼び出し形を後段(sema/codegen)でパターンマッチしているコードが壊れる(引数の型アサーションが失敗する等)。パーサーは「呼び出し対象の名前」を一切見ずに構文を組み立てる(name-agnostic)という設計を保っていたからこそ、この糖衣がどの呼び出しにも無差別にかかってしまった。

対策は、糖衣の展開位置を**パーサーからcodegenへ移し、しかも「予約名・ビルトイン・メソッド呼び出し糖衣・直接のGo資産呼び出しが、いずれも自分の呼び出し形を消費し終えて何も該当しないと確定した後」という、パイプライン上最も遅い地点まで遅らせる**ことだった。この地点まで到達した呼び出しは、構造的に「ただのWeave関数値への適用」以外ではありえないと保証されており、他のどんな予約形の意味も壊しようがない。

**一般化した教訓**: 「予約された形」をパターンマッチで特別扱いする設計(構文キーワードを増やさない代わりに、後段のAST形状マッチングに可視性の責務を委ねる設計)を採用しているフロントエンドに、後から**呼び出し全体に無差別にかかる構文糖衣**(引数の暗黙補完・暗黙変換など)を追加する場合は、その糖衣を「あらゆる予約形が自分の呼び出し形への優先権を使い切った、後に残った呼び出しにしか触れない地点」まで遅らせて実装すること。パーサー段階での機械的な糖衣展開は、一見単純に見えても、既存の「呼び出し形そのものに意味を持たせている」他の予約パターンと静かに衝突する——ビルドは通っても実行時(またはsemaの型チェック)まで発覚しないため、フルリグレッション(全サンプルの実地検証)を回して初めて見つかるバグになりやすい。

## 15. コンパイル時に確定した識別子への参照は、reflectを必要としない(§6の拡張)

§6で「Go資産の名前解決は静的にできても、呼び出し自体はWeave変数が`^any`である以上reflect経由になる」と結論づけたが、これは**「呼び出し対象を動的に検索する必要がある」場合の話であって、「参照する識別子自体がコンパイル時に完全に確定している」場合には当てはまらない**、という区別を後日のGoパッケージレベル変数読み取り機能(`govar`)で発見した。

§6は`value`オペランドカテゴリの`?xxx`/`?xxx.yyy`トークンが「関数値そのものを、呼び出し対象としてではなく普通の引数値として渡せる」ことを発見していたが、この事実は**関数に限らない**——Goのパッケージレベル変数(`os.Stdout`のような)を指す`?pkg.Var`というトークンも、全く同じ形で「そのままCALLの引数として埋め込める、生のGo識別子参照」として通る(直接probeで確認済み)。読み取り側のヘルパー関数(Weaveでは`weavert.NormalizeGoValue(v any) any`——数値正規化のためだけの既存関数をそのまま再利用できた)はreflectを一切使わない普通のGo関数のままでよく、reflectが要る理由が最初から存在しない。

**一般化した教訓**: 「動的型付け言語だからGo資産境界は常にreflectを要する」という判断は誤りではないが、**その理由を正確に言うなら「呼び出し対象・メソッド名など、解決すべき『名前』自体が実行時まで確定しない場合」に限られる**。参照する対象がコンパイル時に完全に確定している識別子(パッケージ変数、あるいは関数そのもの)であれば、`?pkg.xxx`という値トークンをそのまま埋め込むだけで、reflectを一切経由しないネイティブな`CALL`が書ける。動的型付け言語のGo資産連携を設計する際は、「名前解決が静的かどうか」と「値そのものが動的型(`^any`)かどうか」を混同しないこと——後者は前者を必ずしも意味しない。

副次的な発見として、**「参照するたびに値をキャッシュせず、その都度新しいCALLを生成する」という素朴な実装から、意図せず「ライブな参照」(宣言時の1回のスナップショットではなく、参照時点の実際の値を毎回返す)という意味論が自然に得られた。** 生成されたGoコードは`pkg.Var`という式をその参照箇所でそのまま評価するため、キャッシュする理由も機構も無ければ、放っておいても「常に最新の値を読む」動作になる。逆に「宣言時の1回だけ評価してスナップショットを保持する」意味論が欲しい場合は、宣言の時点で明示的に`VAR`へ`SET`し、以降の参照はそのVARを経由させる必要がある——**この2つの意味論はどちらも実装コストが低く、どちらを選ぶかは意図して決めるべき設計の分岐点であり、片方が「自然に出てくる」からといって無意識に選ばないこと。**

## 16. コンテナ系の型ヒント: リテラル型トークンが使えない制約は「1つの名前付き型」で回避できる

AMIVMの`type1`オペランドはスライスのリテラル形(`^[]byte`のような)を直接は受け付けない(「型として解釈できない形式です」というamivm自身のエラーで直接probe確認済み)——これは§1・§2で確立した「コンテナ系命令は`^any`一本化と相性が悪い」という結論の一種だが、**今回はコンテナそのものを回避するのではなく、コンパイラ自身が1つだけ名前付き型を発行することで、リテラル形が使えない制約だけを迂回できた**。

具体的には、`SLTYPE ^GoBytes ^byte`という宣言をプログラム全体で(必要になった時点で)1回だけトップレベルへ発行し(`^[]byte`という型ヒントが要求されるあらゆる箇所で使い回す、重複排除込み)、以降このプログラム内では`^GoBytes`という名前付き型を、まるで`^[]byte`であるかのように使う。これが成立するのはGoの代入可能性規則(assignability)のおかげ——**名前付き型と無名型の間では、少なくとも片方が無名で、かつ両者の基底型が一致していれば、変換無しで相互に代入できる**。この結果:

- Go標準ライブラリの関数が返す無名の`[]byte`戻り値は、`^GoBytes`型で宣言したCALLの結果代入先へ**変換コード無しでそのまま**代入できる
- `^GoBytes`型の値も、`[]byte`引数を取るGo関数へ**変換コード無しでそのまま**渡せる

変換が必要になるのは、この名前付き型と「フロントエンド言語自身のネイティブな値表現」との間を橋渡しする、ただ1箇所だけ——Weaveの場合は「バイト列という概念を持たず、文字列型しか無い」ため、`^GoBytes`⇔`^string`の変換(`?string`/`?GoBytes`という型名そのものをcallnameに使うネイティブなキャストCALL、どちらも1行)が必要だった。

**一般化した教訓**: 将来別のAMIVMフロントエンドが、スライス以外のコンテナ型(map、配列等)を型ヒントとして必要とする場面でも、同じ戦略が使えるはずである——**「リテラルの無名コンテナ型をそのままIRへ埋め込もうとする」のではなく、「対応する`*TYPE`系命令(`SLTYPE`/`MPTYPE`/`STTYPE`等)で名前付き型を1つだけ発行し、Goの代入可能性規則に委ねて無名型と同じように振る舞わせる」**という迂回策を先に検討する価値がある。

もう1点、実装上のハマりどころとして記録しておく——**「この型ヒント文字列は特別なコンテナ形を表しているか」を判定するロジックは、文字列をAMIVM型トークンへ変換する前の、元の宣言文字列に対して行う必要がある。** 一度`"?[]byte"`→`^GoBytes`のようにトークン化してしまうと、その文字列だけを見ても「これはスライス型ヒントの変換結果なのか、たまたま`GoBytes`という名前で宣言されたユーザー定義型なのか」を区別できなくなる(変換後は同じ文字列になるため)。変換前の元の文字列を判定用に保持し、変換後のトークンは純粋にIR出力のためだけに使う、という責務の分離が必要——**「変換」と「意味論的な分類判定」を同じ文字列の同じ地点で済ませようとすると、変換後に情報が失われて判定不能になる**、という一般的な罠。

## 17. 名前付き型の代役は「値の受け渡し」までしか救えない——関数型を構成する要素になった途端、Goは完全な型の同一性を要求する

§16で確立した「名前付き型で無名のコンテナ型を代役する」戦略には、実装するまで気づかなかった重大な限界があった。**この限界は、AMIVMの`FGET`(構造体フィールド取得と、レシーバーに紐づいたメソッド値の取得の両方に使われる、`single1 = variable.field`という既存VARへの代入を生成する命令)と組み合わせた時にだけ表面化する。**

`FGET`の生成コードは「既に`VAR`で宣言済みの変数への、ふつうの代入」であり、代入先の変数の型(AMIVMの`FNTYPE`で宣言した関数型)と、代入元の式(`variable.method`、実際のメソッドの型)が一致している必要がある。Goの代入可能性規則(§16の「片方が無名なら基底型が同じでよい」)は、**トップレベルの値そのもの**を代入する場合には効くが、**関数型を構成する要素(引数・戻り値の型)として現れる場合**には効かない——関数型同士の比較は「対応する引数・戻り値の型が全て**完全に同一**(identical)であること」を要求し、この「同一性」判定は代入可能性よりずっと厳しく、名前付き型と無名型は基底型が一致していても常に別の型として扱われる(直接probeで確認: `type GoBytes []byte; type Fn func() GoBytes`と宣言した`Fn`型の変数へ、実際の型が`func() []byte`のメソッド値を代入しようとすると、コンパイルエラーになる)。

**教訓を一般化すると: 名前付き型による代役(§16の戦略)が安全に使えるのは、その値が「ただの値として代入・受け渡しされる」場面に限られ、「関数の引数・戻り値の型として、より大きな型(関数型)を構成する一部になる」場面では通用しない。** AMIVMで関数値・メソッド値を扱う命令(`FNTYPE`+`FGET`のような、事前に宣言した関数型に対して値を代入する形の命令)と、名前付き代役型が必要なコンテナ型ヒント機能を組み合わせる設計をする際は、この「値としての代入」と「型としての合成」の違いを最初に意識すること——後から気づくと、AMIVM本体に手を入れない限り迂回できない場合がある(次節参照)。

同じ問題は、この限界を回避する側(フロントエンド自身のヘルパー関数)を書く際にも再発する: **Goの型スイッチ(`switch v.(type) { case []int: ... }`)も、代入可能性ではなく完全な動的型の一致を要求する**——名前付き代役型で宣言された変数の値を`any`へboxすると、その動的型は(元が無名の`[]int`だったとしても)box時点の**その変数自身の静的型**(名前付き型)になるため、`case []int:`には決して一致しない。この問題を「型スイッチで実行時に振り分ける」設計で解決しようとすると同じ壁に当たる——正しい回避策は、型スイッチ(動的な振り分け)ではなく、**コンパイル時に静的に確定している型ごとに、具体的な型を引数に取る専用の関数を用意し、呼び出す関数自体をコンパイル時に選ぶ**こと(値の受け渡しは代入可能性で解決するので、名前付き代役型のままで問題なく動く)。§18で詳述する。

## 18. AMIVM本体への機能追加をユーザーと協働設計した実例——提案→懸念の指摘→再設計、という往復

§17の壁(`FGET`+名前付き代役型の組み合わせが構造的に壊れる)は、Weave側の回避策だけでは解決できない種類の問題だった——`FGET`自体が「既存VARへの代入」という形を取る以上、代入先の型を「実際のメソッドの型と完全に一致する」ように**宣言する**手段が無い限り(AMIVMの`type1`にリテラルの関数型・スライス型を書けない、という制約と表裏一体)、正攻法の解決策が無かった。ここで初めて、実装を中断してユーザーに「AMIVM側の修正を含めた解決案はあるか」と相談し、**新しいAMIVM命令をその場で協働設計する**という展開になった。

**このプロジェクトでAMIVM本体への機能追加が起きたのは`CLOS`のネスト許可(前半・後半完了後)に続いて2度目だが、進み方が大きく異なった。** `CLOS`のネスト許可は、ユーザーが(このセッションの外で)先に設計・実装を終えてから「使えるようになった」と伝えられる形だったのに対し、今回は**設計そのものをリアルタイムの対話で往復させながら詰めた**——

1. フロントエンド側(このエージェント)から「`FGET`の`=`を`:=`に変えれば型の完全一致要求を回避できるはず」という**方向性**(何が原因で、どう直せば直るか)を提案
2. ユーザーから`EXFN local exname → local := exname`(外部パッケージ関数専用)という**具体的な命令案**が返ってきたが、これは対象が「静的なパッケージ関数」であり、実際に壊れていた「レシーバーに紐づく動的なメソッド値」とは対象が違うと気づき、その差分を指摘
3. ユーザーがその指摘を踏まえて`METHOD local variable method → local := variable.method`という、まさに必要だった形へ設計を更新
4. 「メソッド以外への誤用を防ぎたい」という追加要望に対し、既存の識別子プレフィックス体系(`>`=フィールド名)に倣った専用プレフィックスの新設を提案し、ユーザーが`<`(メソッド名)として確定・実装

**この往復から得られる教訓: 別の実装者(人間)が保守するIR基盤に変更を依頼する際、フロントエンド側が把握しているのは「症状」と「壊れている理由」までで、「対象範囲を正確に切り分けた解決策」は相手の最初の提案が的を外すことがある、という前提で臨むとよい。** 今回、最初の`EXFN`案は「外部関数の束縛」という一般的すぎる枠で捉えられていたが、実際の問題は「メソッド値の抽出」というもっと狭い、しかし別の性質(レシーバーという実行時の値に依存する)を持つケースだった——フロントエンド側がこの違いを具体的に(なぜ`gofunc`の直接呼び出しは無傷で、`gomethod`のFGETだけが壊れるか)説明できたことで、1往復で正しい設計に収束できた。**症状の再現条件(この場合は「名前付き代役型を関数型の構成要素として使う」という具体的な組み合わせ)を正確に言語化できることが、外部の実装者との協働を早く終わらせる鍵になる。**

**新命令の設計が固まった後も、実装前に直接probeでの検証を欠かさなかった。** `METHOD`(`:=`型推論)が本当に問題を解決するかを、AMIVM側の実装完了を待つ前に素のGoコードで検証した(名前付き型の引数を渡す・名前付き型の変数へ戻り値を代入する、という一連の流れが実際にコンパイルできるか)。AMIVM側の実装が完了した後も、実際の`amivm`バイナリで手書きIRを流し、生成されたGoコードが期待通り`local := recv.Method`という形になり、`go build`まで通ることを確認してから、初めてフロントエンド(Weave)側のコード生成を書き換えた——このプロジェクトが一貫して守ってきた「実装前にprobeで検証してから着手する」規律を、**自分のリポジトリの外(AMIVM本体)の変更に対しても同じ厳格さで適用した**ことになる。

最後に、対応していないコンテナ形(Weaveでは`"?[]byte"`以外の全スライス型)は、**IR化する前のsema層で明確なコンパイルエラーとして拒否すること。** 放置すると、無効なAMIVM型トークンがそのままIRへ出力され、amivm自身の分かりにくいエラーとしてユーザーに跳ね返る——「IR生成そのものの構文的正しさはamivm呼び出し前にフロントエンド側で検査する」という原則は、こうした部分的にしか対応していない型ヒントの検証にもそのまま当てはまる。

## 19. `DEFER`は「`weavert.Call`経由の間接呼び出しでは意味が無い」唯一の組み込み関数だった

Weaveのほぼ全ての組み込み関数(`spawn`/`send`/`len`/`raiseIfError`等)は、素朴な`?weavert.Xxx`への`CALL`(ワンステップの間接呼び出し)で実装できる——これはこのプロジェクト全体を貫く、迷ったらまず試す既定の実装パターンだった。パニック捕捉機能(Goの`defer`+`recover()`に対応する`recover(handler)`)を実装する段になって、**このパターンが通用しない初めての組み込み関数**に出会った。

理由はGoの`recover()`自身の性質にある: `recover()`は、それを**直接**呼び出した関数が`defer`によって登録されている場合にしか効果を持たない(呼び出しチェーンを1段でも挟むと`recover()`は常に`nil`を返す、というGo言語自体の制約)。そのため、パニックを実際に捕まえるヘルパー関数(`weavert.RecoverPanic`のような、内部で`recover()`を呼ぶ関数)は、それ自身が本物の`defer`で登録されていなければならない——「`weavert.Call`のようなヘルパーへ間接的に渡す」という、他の全ての組み込み関数で機能してきたパターンが、この1つだけ機能しない。

対策は単純だった: `recover(handler)`だけは、他の組み込み関数のような`?weavert.Xxx`への`CALL`ではなく、AMIVMのネイティブな`DEFER ?weavert.RecoverPanic <handler>`命令(`defer weavert.RecoverPanic(handler)`を生成する)へ直接変換する。`DEFER`の引数(`handler`)は他のオペランドと同じく即座に評価される(Go自身の`defer f(x)`が`x`を即座に評価し、呼び出しだけを遅延するのと同じ)ため、`handler`がリテラルの関数式(`fn(msg) {...}`)であっても、通常の`genExpr`で先に評価してから`DEFER`のオペランドとして渡すだけで済み、コード生成上の特別な扱いはこの1点(「`CALL`ではなく`DEFER`を使う」という選択そのもの)に尽きた。

**教訓を一般化すると**: 「動的型付け言語の値は全て`any`なので、ほぼ全ての操作はGoランタイムへの間接呼び出し(`CALL`)に一元化できる」という、このプロジェクトが徹底してきた設計判断(§1)は、**対象の言語機能がGo自身の言語機能(ここでは`defer`+`recover()`)を直接借用している場合には当てはまらないことがある**——Goの`recover()`のように「呼び出し階層そのものに意味を持つ」組み込み機能を橋渡しする際は、ラッパー関数越しの間接呼び出しにできるかどうかを実装前に確認すること。この特性(直接呼び出しの階層に依存する)を持つGo組み込み機能は`recover()`以外にも存在しうる(例えば`runtime.Caller`のスキップ数のような、呼び出し階層の深さに依存する機能)ため、次にAMIVM上へ動的型付け言語を実装する際、同種のGo言語機能を橋渡しする場面があれば、まずこの制約を疑うとよい。

## 20. 固定アリティ用ヘルパーが揃っていれば、大量の同系統ビルトインは「配線するだけ」で増やせる

`recover(handler)`(§19)の直後、性質の異なる20個の組み込み関数(文字列操作11個・数学関数8個・プロセス即時終了`exit`)をまとめて追加する機会があった。コード生成側で新しく設計した要素は、**3引数固定アリティ用のヘルパー(`genThreeArgBuiltin`)をもう1つ追加しただけ**だった——既に確立済みだった1引数用・2引数用ヘルパー(`genOneArgBuiltin`/`genTwoArgBuiltin`、Step2〜3で導入)に、「ビルトイン名→対応する`weavert`関数名」というマッピングをdispatchテーブルへ1行ずつ追記するだけで、20個のうち18個(3引数の`substring`/`replace`以外の全て)はそのまま配線が完了した。`recover`が「`CALL`一本化パターンが通用しない唯一の例外」(§19)だったのに対し、この20個は例外を1つも生まなかった——「ほとんどの組み込み関数は素朴な`CALL`一本化パターンに収まり、Go自身の言語機能を直接借用するものだけが例外になる」という§19の教訓の裏付けにもなった。

これは§13の「N=2専用の特別扱いをやめてN値の一般形に倒す」教訓の、また別の現れ方でもある——**「固定アリティごとに1つの汎用ヘルパーを用意しておけば、同じアリティの新しいビルトインを追加するコストは『dispatchテーブルへの1行追加』まで下がる」**、という設計上の複利効果。新しいビルトインを実装する労力の重心は、コード生成側ではなくランタイム層(`weavert`)の関数実装・エラーメッセージの設計・単体テスト・仕様書の記述の方へ完全に移る。

**教訓を一般化すると**: 「多数の組み込み関数をまとめて追加する」という機会が来たら、まずコード生成側に必要な固定アリティ(1引数・2引数・3引数、...)のヘルパーが既に揃っているかを確認するとよい。揃っていれば、コード生成側の設計作業はほぼゼロ(足りないアリティが1つあれば、それだけ新設すればよい)で済み、見積もりの大半をランタイム層とドキュメントに割り振れる——動的型付け言語で`^any`一本化+ランタイム関数への`CALL`集約(§1)を選んだ設計は、単に「動く」だけでなく、こうした横展開のコストを継続的に下げる効果も持つ、という副次的な利点として記録しておく。
