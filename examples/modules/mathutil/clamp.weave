Clamp = fn(v, lo, hi) {
    if v < lo { return lo }
    if v > hi { return hi }
    return v
}

square = fn(x) { return x * x }   // 小文字始まりなので他パッケージから見えない

ClampSquare = fn(v, lo, hi) {
    return square(Clamp(v, lo, hi))
}
