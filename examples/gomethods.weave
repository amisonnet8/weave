func main(): int {
	GoReader = gotype("?*strings.Reader", {
		len: gomethod("Len", "?int")
	})
	newReader = gofunc("?strings.NewReader", GoReader, "?string")

	r = newReader("hello, weave")
	print(r.len())
	print(r.len() + 8)
	print(r.len() > 10)

	return r.len()
}
