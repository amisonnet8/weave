# Weave 言語仕様

> AMIVM(独自の中間言語からGoコードを生成するコンパイル基盤)向けの3つ目のフロントエンド言語。
> Seed(素直な手続き型)・Cascade(既存パラダイムの寄せ集め)に続き、Weaveは**前提を1つ疑うこと**を目的にした言語。
> 「関数呼び出し」「オブジェクトの継承」「並行プロセス間の通信」という3つの別々の概念を、
> **1つの仕組み(名前によるプロパティ検索)** の上に統一する。

## 0. 核となる着想

Weaveでは、次の3つが**同じ操作**として扱われる。

1. オブジェクト`obj`のプロパティ`x`を読む(`obj.x`)
2. プロトタイプを継承したオブジェクトが、親の持つ関数`greet`を呼ぶ(`obj.greet()`)
3. アクター`a`に`"increment"`というメッセージを送る(`send(a)("increment")(5)`)

これらは全て「オブジェクト(map)に対して名前で検索し、見つかった値を使う」という**1つの仕組み**の表れである。アクターへのメッセージ送信は、実装上「アクターが持つ内部オブジェクトから、メッセージ名と同じ名前のプロパティ(関数)をプロトタイプチェーン経由で検索し、`self`と引数を渡して呼び出す」ことでしかない。つまり**メッセージハンドラの定義は、オブジェクトのメソッド定義と全く同じ構文で書く**。

## 1. 字句規則

- 改行が文の区切り(セミコロンは不要)
- `//`以降、行末までをコメントとして無視する
- `/* ... */`(複数行コメント)も書ける——**構文はGoの`/* ... */`にそのまま準拠する: ネストしない(最初に現れた`*/`で閉じる。`/* /* */ */`と書くと、内側の`/* */`で閉じてしまい、末尾の` */`はコメントの外に取り残されて構文エラーになる)、行の途中にも書ける(`x = /* コメント */ 5`のような形も有効)。**複数行にまたがる`/* ... */`自体は改行トークンを生成しない**——コメントの中に何行分の改行が含まれていても、文の区切りとしては扱われない(1つの空白と同じ扱い)。文を区切るのは、コメントの外にある実際の改行だけ
- ソースファイルの拡張子は`.weave`
- キーワード・識別子は大文字小文字を区別する

### 1.0 識別子・数値リテラルの字句

- **識別子**は`[a-zA-Z_][a-zA-Z0-9_]*`(ASCIIの英字・数字・アンダースコアのみ、先頭は数字不可)。**Unicode文字(`あ`のような非ASCII文字)は識別子に使えない**——変数名・関数名・プロパティ名は常にASCII識別子。ただし文字列リテラルの中身は非ASCII文字を自由に含められる(1.1節)
- **数値リテラル**は`[0-9]+`または`[0-9]+.[0-9]+`の形のみ。**指数表記(`1.23e4`のような)・16進/8進/2進リテラル(`0x1F`等)・先頭が`.`で始まる小数(`.5`)は書けない**——`.5`と書くと、パーサーは`.`をプロパティアクセス演算子として読もうとして構文エラーになる。小数は必ず`0.5`のように整数部を書く。負の数値リテラルという構文も無い(`-5`は「数値リテラル`5`に単項`-`を適用した式」であり、字句レベルの1トークンではない——8節)

```
x = 1_000     // 構文エラー(アンダースコア区切りは無い)
y = .5        // 構文エラー(先頭の.はプロパティアクセスと解釈される)
z = 1e10      // 構文エラー(指数表記は無い)
w = 0.5       // OK
v = -5        // OK(単項マイナス演算子+リテラル5)
```

### 1.1 文字列リテラルのエスケープシーケンス

文字列リテラル(`"..."`)は改行をまたげない(1行で閉じる必要がある)。`\`から始まるエスケープシーケンスで、以下の文字・値を表現できる。**構文はGoの文字列リテラル(二重引用符形式)にそのまま準拠する。**

| 記法 | 意味 |
|---|---|
| `\a` `\b` `\f` `\n` `\r` `\t` `\v` | ベル・バックスペース・改ページ・改行・復帰・水平タブ・垂直タブ |
| `\\` | バックスラッシュそのもの |
| `\"` | ダブルクォートそのもの |
| `\xHH` | 16進2桁のバイト値(0〜255) |
| `\NNN` | 8進3桁のバイト値(0〜255。桁数は常に3桁ぴったり必要) |
| `\uXXXX` | 16進4桁のUnicodeコードポイント |
| `\UXXXXXXXX` | 16進8桁のUnicodeコードポイント |

非ASCII文字(`"あ"`のような)はエスケープ無しでそのまま埋め込める。上記以外の`\`の使い方(存在しないエスケープ文字、桁数不足、8進エスケープの値が255を超える等)はコンパイルエラーになる。Weaveには文字(rune)専用のリテラル型が無いため、Goの`'...'`ルーンリテラルで使える`\'`は文字列の中では使えない(Go自身も`\'`はルーンリテラルの中でのみ有効で、`"..."`文字列リテラルの中では無効)。

## 2. 値と型

Weaveは**動的型付け**言語。変数宣言に型注釈は書かない。値は次の6種類のいずれか。

| 種類 | 例 | 備考 |
|---|---|---|
| 数値 | `1`, `-3`, `1.5` | 整数・浮動小数点を区別しない(内部的にはfloat64相当で統一) |
| 文字列 | `"hello"` | |
| 真偽値 | `true`, `false` | |
| `nil` | `nil` | 「値が無い」を表す唯一の値 |
| オブジェクト | `{ x: 1, y: 2 }` | プロパティの集合(4節) |
| 関数 | `fn(a) { ... }` | 常に1引数(5節)。オブジェクトのプロパティにもなれる |
| アクター参照 | (リテラルは無い。`spawn(...)`の戻り値) | 6節 |

すべての変数は宣言時にゼロ値を持たず、代入されるまで`nil`。数値を整数・浮動小数点で区別しないのは、動的型付けの中で型の組み合わせ爆発(int+float等)を避けるための単純化。

**「真偽値として扱えるのは`true`/`false`だけ」という制約が厳格に適用される。** JavaScriptの`if (0)`やPythonの`if []`のような「真偽値以外の値を条件式に置くと自動的にtruthy/falsyへ変換される」という仕組みはWeaveには無い。`if`/`while`の条件式、`&&`/`||`の両オペランドは、実際に`true`/`false`でなければ実行時エラーになる(数値の`0`もオブジェクトも`nil`も、条件式としてはエラー)。

```
if 5 {}       // 実行時エラー: 条件は真偽値でなければならない
if nil {}     // 同様にエラー
if x == nil {} // OK — 比較演算の結果(bool)を条件にする
```

値の等価性(`==`)・順序比較(`<`等)の詳しい規則は8節を参照。特に**オブジェクト同士・関数(クロージャー)同士を`==`で比較することはできない**(19節「現状の仕様でできないこと」)。

## 3. リテラル

```
nil
true
false
1234
1.5
"hello"
"say \"hi\"\n"           // エスケープシーケンス(1.1節)
{ }                      // 空オブジェクト
{ x: 1, y: 2 }            // プロパティを持つオブジェクト
{ __proto__: base, x: 1 } // プロトタイプ指定付きオブジェクト(4.2節)
fn(a) { return a + 1 }    // 関数リテラル
```

配列相当の値は「連番の数値キー(`0`, `1`, `2`, ...)を持つ普通のオブジェクト」として表現する(専用の配列型は持たない)。組み込み関数`list(...)`(11節)がこの形のオブジェクトを作る糖衣構文になっている。**`[1, 2, 3]`のような角括弧の配列リテラル構文自体は存在しない**——配列が欲しい場合は必ず`list(1, 2, 3)`と書く。要素の読み書きは次の3.1節を参照。

オブジェクトリテラル`{ }`のキーは常に識別子(1.0節の識別子文法)でなければならない——`{"my-key": 1}`のような文字列リテラルキーや、`{ [expr]: 1 }`のような計算キーは書けない(19節)。フィールドはカンマ区切りで、複数行に分けて書いてもよい(末尾カンマも許される)。

```
config = {
    name: "weave",
    version: 1,
}   // 末尾カンマ・複数行、どちらもOK
```

### 3.1 リストの添字アクセス —— `list[index]`

```
nums = list(10, 20, 30)
print(nums[0])       // 10。at(nums, 0)と全く同じ意味
nums[1] = 99          // 既存の位置への書き込み
print(nums[1])        // 99
```

`list[index]`(角括弧)は`at(list, index)`(11節)への構文糖衣で、`list[index] = value`という書き込みも(角括弧構文でのみ)できる——`at(...)`自体には対応する書き込み関数は無い。`.`によるプロパティアクセスは識別子が数字から始まれない(1節)ため数値位置には使えないが、角括弧はこの制約を受けない。`index`は数値リテラルに限らず、変数や式も書ける(`nums[i]`、`nums[i + 1]`)。

- **角括弧構文が意味を持つのは、この「既存の数値位置への読み書き」だけ。** `obj[key]`のような、識別子以外の計算した文字列でオブジェクトの任意のプロパティへアクセスする構文(計算されたプロパティアクセス)は、`list[index]`とは別物であり、依然として存在しない(4.1節・19.1節)——`weavert.ObjAt`/`ObjSetAt`はどちらも数値インデックスの読み書き専用で、文字列キーには使えない
- **書き込みは、その位置が既に存在する場合にのみ成功する。** `at(...)`自身の「範囲外はその場でエラー」という仕様(11節)と対称に、`list[index] = value`も範囲外の位置への書き込みをエラーにする——`list(...)`/`at(...)`には要素の追加・削除・挿入・並べ替えといった配列操作が無い(11節)という制約は、角括弧での書き込みにもそのまま引き継がれる。位置を増やしたい場合は改めて`list(...)`で新しいlistを作る
- `[i][j]`のように連続して書ける(`.`や`(...)`と同じ、左から右への後置演算子として働く)。`matrix[0][1] = 5`のように、読み取りと書き込みを組み合わせて多次元的に使うこともできる
- `.`によるプロパティアクセス・メソッド呼び出しと自由に混在できる(`obj.items[0]`、`list[0].name`、`list[0].method()`)
- **listの各位置は、内部的には固定桁数のゼロ埋め文字列(`"0"`ではなく`"0000000000"`)をキーとして保持している。** これは`for k, v in list`(7節)が文字列としての辞書順で列挙しても数値としての大小順に一致させるための実装上の工夫で、`at(...)`/`list[index]`/`has(...)`/`remove(...)`のいずれも、ゼロ埋め前の普通の数値・文字列(`0`や`"0"`)を渡すだけで正しい位置を指せる——ゼロ埋めはWeaveプログラムからは一切見えない内部表現であり、`for k, v in list`で得られる`k`もゼロ埋め前の元の文字列に戻される

## 4. オブジェクトとプロトタイプ

### 4.1 基本

オブジェクトは`{ }`で作り、プロパティは`.`でアクセスする。プロパティは実行時に自由に追加・削除できる。

```
point = { x: 1, y: 2 }
point.z = 3          // 追加
point.x = 10          // 更新
remove(point, "y")     // 削除(11節)
```

存在しないプロパティを読むと(自身にも、4.2節のプロトタイプチェーンのどこにも無ければ)、エラーにはならず`nil`が返る。

```
p = { x: 1 }
print(p.y)   // nil(エラーにはならない)
```

プロパティ名は`.`の後ろに直接書く識別子だけで、実行時に計算した文字列でアクセスすること(いわゆる「計算されたプロパティアクセス」、`obj[someVar]`のような構文)はできない——書き込みも同様(19節)。

### 4.2 プロトタイプチェーン

`__proto__`という予約プロパティで、オブジェクトの親(プロトタイプ)を指す。プロパティ読み取り(`obj.name`)は次の順で探す。

1. `obj`自身が`name`というプロパティを持っていれば、その値を返す
2. 持っていなければ、`obj.__proto__`が存在する限り、それを対象に1へ戻る(再帰)
3. どこにも見つからなければ`nil`を返す

プロパティの**書き込み**(`obj.name = v`)は常に`obj`自身に対して行う(親を書き換えることはない)。

```
base = {
    greet: fn(self) { print(self.name + " says hi") }
}

alice = { __proto__: base, name: "Alice" }
alice.greet()     // baseのgreetを継承して呼べる(9節の自己注入糖衣構文——alice自身が自動でselfに渡る)
```

上のコードは`alice.greet(alice)`のように明示的に`alice`をもう一度渡す**必要は無い**——9節で述べる自己注入糖衣構文が、`.method(...)`という呼び出しの第1引数として`obj`自身を自動的に差し込む。`greet`が`fn(self) {...}`という1引数関数である場合、`alice.greet(alice)`と書いてしまうと`greet(alice)`の呼び出しが完了した時点(戻り値は`nil`)でさらに`alice`を渡そうとし、「関数でない値は呼び出せない」という実行時エラーになる(19節参照)。

`__proto__`自体も普通のプロパティなので、`alice.__proto__`と読めば`base`そのものが返り、`alice.__proto__ = otherBase`と書けば、オブジェクト生成後でも後からプロトタイプを差し替えられる。

```
alice.__proto__ = { greet: fn(self) { print("hi, " + self.name) } }
alice.greet()   // 差し替えた後のプロトタイプが使われる
```

`has(obj, name)`(11節)と`for k, v in obj`(7節)は、**`obj`自身が持つプロパティだけを見る**(プロトタイプは辿らない)。一方`obj.name`による読み取りと`obj.method(...)`によるメソッド呼び出しは、プロトタイプチェーン全体を辿る。この非対称性(「読み取りは継承する、列挙は継承しない」)は意図的な仕様——`for k, v in alice`は`base`由来の`greet`を列挙しない。

`__proto__`の循環(`a.__proto__ = b; b.__proto__ = a`)はコンパイラ・ランタイムのどちらでも検出しない。作ってしまった場合、プロパティ検索は無限ループする(意図的にチェックを入れていない。プロトタイプチェーンは通常ごく浅いため、実務上は問題にならない)。

### 4.3 型ヒント(任意)——`shape`/`checkShape`

オブジェクトが期待通りのプロパティを持っているかを、任意のタイミングで検証できる。15.4節の`gotype`/`gomethod`/`gofunc`の型ヒントと同じ位置づけ(TypeScriptに対するJavaScript、Pythonの型ヒントと同じ——実行速度のためではなく、安全性・保守性のための機能)だが、対象がGoの型ではなくWeave自身の値の種類である点が異なる。

```
PointShape = shape({ x: "number", y: "number" })

p = { x: 1, y: 2 }
checkShape(PointShape, p)   // pの各プロパティが期待通りの種類か検証。合わなければその場でエラー
```

`shape({ プロパティ名: "種類", ... })`は、期待するプロパティの種類をコンパイル時に登録するだけの宣言(15.1節の`gotype`と同じく、実行時の値は持たない)。`checkShape(宣言名, 値)`は、その値の各プロパティを実際に検証する——宣言していないプロパティ、あるいはオブジェクトが余分に持っているプロパティはチェックしない(あくまで宣言した項目についてのみの検証)。**プロパティが丸ごと欠けている場合も「型が違う」扱いになる**(存在しないプロパティを読むと4.1節の通り`nil`になり、`nil`は`"number"`等どの種類にも一致しないため)——「このプロパティは省略可能」という宣言方法は無い(全て必須項目として扱われる、19節)。

`"object"`という種類の検証は、値がオブジェクト(`{}`で作った値)であることを確認するだけで、そのネストしたオブジェクト自身が期待する形をしているかまでは検証しない(`shape`は1階層だけの検証で、再帰的なネストしたshape宣言という仕組みは無い)。ネストした値も検証したい場合は、その値に対して別途`checkShape`を呼ぶ必要がある。

`"種類"`に書けるのは次の5つ。

| 種類 | 対応するWeaveの値 |
|---|---|
| `"number"` | 数値 |
| `"string"` | 文字列 |
| `"bool"` | 真偽値 |
| `"object"` | オブジェクト({}で作った値、ネストしたオブジェクトも含む) |
| `"function"` | 関数(クロージャー) |

不一致があれば`checkShape`を呼んだ場所で即座に停止し、どのプロパティで・何を期待して・実際は何だったかが分かるエラーになる。

`number`/`string`/`bool`/`object`は、AMIVMの型アサーション(`ASSERT`)で実際にその場で厳密に検証される——15.4節のGo資産境界の型ヒントと全く同じ仕組み。`function`だけは事情が異なり(Weaveのクロージャーは無名のGo関数型として生成されるため、Goの型アサーションで直接判定できない)、専用の軽量な判定に頼る。いずれの場合も、検証そのものは値を実際に呼び出したり複製したりしない、その場限りの確認である。

## 5. 関数とカリー化

**Weaveの関数は常に1引数を取る。** 複数引数に見える呼び出し`f(a, b, c)`は、`f(a)(b)(c)`(1引数関数を3回連続で呼ぶ)の糖衣構文でしかない。

```
add = fn(a) fn(b) { return a + b }

add5 = add(5)      // 部分適用。「bを待つ関数」が返る
result = add5(3)    // 8

result2 = add(5)(3)  // 上と同じ。add(5, 3) とも書ける(糖衣構文)
```

複数引数のように見える定義`fn(a, b) { ... }`も認められており、これは`fn(a) fn(b) { ... }`の糖衣構文(定義側でも呼び出し側でも、多引数の書き方は単に「1引数関数の連鎖」を短く書けるようにしただけ)。引数の個数に上限は無く、3個以上でも同じ規則で連鎖する。

```
add = fn(a, b) { return a + b }   // fn(a) fn(b) { return a + b } と同じ

clamp = fn(v, lo, hi) {   // fn(v) fn(lo) fn(hi) {...} と同じ
    if v < lo { return lo }
    if v > hi { return hi }
    return v
}
clamp(15, 0, 10)     // 10
clampTo0_10 = clamp(0, 10)   // 部分適用は3引数の関数でも同様。vだけ待つ関数が返る(先頭からしか部分適用できない点に注意)
clampTo0_10(15)       // 10
```

関数はオブジェクトのプロパティにもなれる(4.2節の`greet`のように)。

**`fn() {...}`(引数無しの定義)・`f()`(引数無しの呼び出し)という書き方もできる——ただし、これは糖衣構文であって、「Weaveの関数は常に1引数を取る」というルール自体が崩れているわけではない。** `fn() {...}`は`fn(_) {...}`(10節で述べる、読み取り不可の特別な名前`_`を引数名に使った形)の、`f()`は`f(nil)`の、それぞれ単純な書き換えでしかない。

```
always5 = fn() { return 5 }   // fn(_) { return 5 } と同じ
always5()                      // 結果は5。always5(nil) と同じ
```

**ただし、`obj.method()`(9節のメソッド呼び出し糖衣構文)には、この`f()`→`f(nil)`という書き換えは適用されない。** `obj.method()`は元から「`self`(=`obj`)自体が唯一の引数として自動的に渡る」という意味を持っており、そこにさらに`nil`を差し込んでしまうと、`self`+`nil`の2引数を渡すことになって既存の意味が壊れてしまう。`obj.method()`は今まで通り「引数0個・`self`のみ」を表す特別な形のまま。`obj.method(a)()`のように、メソッド呼び出しの**結果**をさらに引数無しで呼び出す場合は(その`()`の呼び出し対象は`obj.method`という直接のプロパティアクセスではなく、既に評価済みの値なので)、通常通り`f()`→`f(nil)`の対象になる。

```
obj.method()      // selfのみ(nilは追加されない)
obj.method(a)()    // method(obj, a) の結果に対して、更に nil を適用(通常のf()→f(nil)の対象)
```

関数でない値を関数のように呼び出そうとすると(`x = 5; x(3)`)、実行時エラーになる。

## 6. アクターモデル

### 6.1 スポーン

`spawn(obj)`は、オブジェクト`obj`のコピーを内部状態として持つ**アクター**を新規に起動し、そのアクター参照を返す。アクターは専用の受信キューを持ち、他のコードとは独立して並行に動く。

```
counter = { count: 0 }
a = spawn(counter)
```

### 6.2 メッセージ送信 = プロトタイプ検索の遠隔呼び出し

`send(a)`は「アクター`a`宛の送信関数」を返す(カリー化)。これにメッセージ名と引数を渡すと、アクターの受信キューへメッセージが積まれる。

```
tell = send(a)
tell("increment", 5)     // "increment"というメッセージ名で5を渡す
```

アクター側では、キューから取り出したメッセージについて「**アクターの内部オブジェクトから、メッセージ名と同じ名前のプロパティ(関数)をプロトタイプチェーン経由で検索し、`self`(内部オブジェクト自身)+メッセージの引数を渡して呼び出す**」という処理を、メッセージが来るたびに繰り返す。これは0節で述べた統一原理そのもので、**アクターの「メッセージハンドラ」を定義する構文は、オブジェクトの「メソッド」を定義する構文と全く同じ**(4.2節の`greet`と同じ書き方で、アクターのメッセージハンドラも書ける)。

```
counter = {
    count: 0,
    increment: fn(self, amount) { self.count = self.count + amount },
    get: fn(self, replyTo) { reply(replyTo, self.count) }
}

a = spawn(counter)
send(a)("increment", 5)
send(a)("increment", 3)
```

**`send(a)(名前, 値)`は常にメッセージ名+ちょうど1個の値、という形に固定されている(可変長引数ではない)。** `send(a)`はカリー化された2段の関数(`fn(name) fn(value) {...}`)であり、`send(a)("foo")`だけでは(値を待つ関数が返るだけで)まだ送信は完了しない。`send(a)("foo", 1, 2)`のように2個以上の値を渡そうとすると、1個目の`1`で送信が完了した後、その結果(`nil`)に対して`2`を適用しようとして実行時エラーになる。ペイロードが不要なメッセージでも、値を省略することはできず、明示的に`nil`を渡す(`tell("ping", nil)`)。複数の値を渡したい場合は、オブジェクトにまとめて1つの値として渡す(`tell("move", { x: 1, y: 2 })`)。

メッセージは**送信した順番通りに**(FIFO)アクターの受信キューへ積まれ、その順番通りに処理される。存在しないメッセージ名を送った場合(プロトタイプチェーンのどこにも見つからない関数)、そのメッセージは黙って無視される。

### 6.3 `tell`と`ask`

`send`は投げっぱなし(fire-and-forget)。返信が必要な場合は、組み込み関数`ask`を使う。

```
result = ask(a)("get")   // "get"メッセージを送り、返信を待って受け取る
```

`ask(a)`は`send(a)`と違い、**メッセージ名だけを取る1段のカリー**(`fn(name) {...}`)——ユーザーが渡す値の引数は無い。`ask`は内部で使い捨ての一時的な受信チャネルを作り、それを最後の引数(慣習的に`replyTo`という名前を使う)としてメッセージ本体に自動的に付け加えてから送信し、その受信チャネルに値が来るまで**呼び出し元をブロックする**。ハンドラ側は組み込み関数`reply(replyTo, value)`で応答する(6.2節の`get`ハンドラ参照)。`reply`を呼ばないハンドラに対して`ask`を使うと、呼び出し元は永遠にブロックする(意図的な制約——`ask`する側とされる側で「返信するかどうか」の約束をコード上で保証する仕組みは持たない)。`ask`にユーザー自身のペイロードを渡す方法自体が無い(19節)——値を渡す必要がある場合は`send`/`reply`を組み合わせて自分で往復を実装する。

### 6.4 並行性の粒度

1つのアクターは、自分宛のメッセージを**常に1つずつ順番に**処理する(同じアクター内で2つのメッセージが同時に処理されることはない)。異なるアクター同士は完全に並行に動く。この性質により、アクター内部の状態(`self`のプロパティ)への読み書きは、アクター内で見る限り競合状態を考えなくてよい。

**`send`は「投げっぱなし」だが、宛先のアクターが別のメッセージを処理中で受け取れない場合は、送信側もその処理が終わるまで一時的にブロックされる。** アクターの受信キューには**バッファが無い**ため、「値をキューに積んですぐ戻る」という動作は、宛先のアクターがちょうど次のメッセージを受け取れる状態(前のメッセージの処理を終えて待機中)の時にしか成り立たない。宛先のアクターが処理中であれば、送信側は(そのメッセージの**処理が終わる**のではなく、あくまで**キューへ渡せる**ようになるまで)待たされる。つまり、時間のかかるハンドラを持つアクターへ`send`を連続して呼ぶと、呼び出し側のコードもそのハンドラの処理時間の分だけ引きずられて遅くなる——`ask`ほど強い同期(返信を待つ)ではないが、「完全に非同期・瞬時」という意味でもない。この意味論を厳密に気にする必要がある場面は稀だが、「`send`は常に一瞬で返る」という前提でタイミングに依存したコードを書かないこと。

### 6.5 メッセージハンドラ内のエラーはプログラム全体を停止させる

**メッセージハンドラの実行中に実行時エラー(型不一致・存在しないメソッド呼び出し等)が起きた場合、そのエラーは「送信したメッセージが失敗する」という形では現れず、Weaveプログラム全体(全てのアクター・`main`本体を含む)がその場で異常終了する。** 1つのアクターだけがクラッシュして他は動き続ける、という部分的な障害分離は無い——アクターは並行性(6.4節)のための仕組みであり、耐障害性(fault tolerance)のための仕組みではない。

```
proto = {
    boom: fn(self, x) { return x + "not a number" }   // 実行時型エラーになる
}
a = spawn(proto)
send(a)("boom", 5)
// このメッセージが処理された時点で、mainがまだ実行中であっても
// プログラム全体が終了コード2で異常終了する
```

エラーを起こしうる処理(15節のGo資産呼び出しなど)をメッセージハンドラの中で使う場合は、ハンドラ自身の中で`raiseIfError`等を使って早めに検知するか、そもそも型が合わない値がハンドラへ渡らないよう呼び出し側で気をつける必要がある。ハンドラ内での「エラーを捕まえて`reply`でエラー値として返す」というパターン自体は書けるが(Weaveの`raiseIfError`は「エラーがあれば即座に停止する」動作しかせず、「エラーを捕まえて値として扱う」ための例外処理構文は無い——19節)、Go資産由来のエラー(`gofunc`/`gomethod`の戻り値、15節)はそもそも`raiseIfError`を呼ばない限りは普通の値として保持できるので、そちらの経路であれば「エラーを検知して`reply(replyTo, {ok: false, message: ...})`のような値を返す」実装は可能。

## 7. 制御構文

if/while/forはSeed・Cascadeと同じ形を踏襲する(独創性を出す対象はデータモデル・実行モデルに絞り、制御構文まで独自にすると学習コストが跳ね上がるため)。

```
if x == 100 {
    y = 100
} elif x == 200 {
    z = 200
} else {
    x = x + 1
}

while i < 10 {
    i = i + 1
}

for k, v in obj {     // オブジェクトの自分自身が持つプロパティを列挙する(プロトタイプは辿らない)
    print(k + ": " + string(v))
}
```

`break`/`continue`は`while`/`for`のどちらでも使え、最も内側のループにのみ作用する。**ただし、関数リテラル(`fn(...) {...}`)は`break`/`continue`にとって越えられない境界になる**——`while`の中で定義した関数リテラルの内側に`break`/`continue`を書くとコンパイルエラーになる。ループの中で定義したクロージャーが後から(場合によっては全く別の文脈で)呼ばれた時に、定義されたときのループへ`break`する、という状況は一貫した意味を持たないため。

```
while true {
    f = fn(x) { break }   // コンパイルエラー: ループの外(関数リテラルの内側)でbreak
}
```

**カウンタ・範囲を使う`for`(他の多くの言語にある`for i = 0; i < 10; i++`や`for i in 0..10`のような形)は無い。** `for`は上記の`for k, v in obj`(オブジェクトの列挙)専用の構文で、**まだ存在しない**数値の範囲を一から回すには`while`とカウンタ変数を自分で書く(下記の例)。**既存の`list(...)`(3節)を要素ごとに処理したいだけなら、この`while`は不要**——`for k, v in list(...)`が`k`(位置)・`v`(値)の両方を順番に渡してくれる(3.1節・本節の下記段落)ので、`while`+手書きカウンタが必要になるのは「まだlistが無い状態から数値の範囲そのものを生成したい」場合に限られる。

```
i = 0
while i < 10 {
    print(i)
    i = i + 1
}
```

`for k, v in obj`の列挙順序は、`obj`が自身に持つプロパティ名を**アルファベット順(文字列としての辞書順)にソートした順**——Go言語のマップ反復のような非決定的な順序ではない。`list(...)`(3節)が持つ連番の数値キー(`"0"`, `"1"`, `"2"`, ...)についても、これは**そのまま数値としての大小順に一致する**——キーは内部的に固定桁数のゼロ埋め文字列(`"0000000000"`のような)として保持されており、辞書順ソートと数値順が自動的に一致するようにしてある(`k`として実際に見える値はゼロ埋め前の元の文字列に戻される、3.1節)。文字列プロパティ(識別子由来のキー)とlistの数値キーが混在するオブジェクトでは、数値キー同士は互いに数値順を保つが、アルファベット文字列キーとの相対順序はASCIIコード順(数字はアルファベットより前)になる。

## 8. 演算子

```
+   -   *   /   %        // 算術演算子(数値のみ。+は文字列結合にも使う)
==  !=                     // 等価比較
<   <=   >   >=            // 順序比較(数値・文字列)
&&  ||  !                  // 論理演算(真偽値)
```

複合代入・インクリメント演算子は持たない(`i = i + 1`と書く。文法要素を削って言語を小さく保つ判断)。

### 優先順位(上から高い順)

| 優先度 | 演算子 |
|---|---|
| 1(最高) | `( )`、`.`(プロパティアクセス)、関数呼び出し`( )`、`[ ]`(添字アクセス、3.1節) |
| 2 | 単項`!`、単項`-` |
| 3 | `*` `/` `%` |
| 4 | `+` `-` |
| 5 | `<` `<=` `>` `>=` |
| 6 | `==` `!=` |
| 7 | `&&` |
| 8(最低) | `\|\|` |

`+`は両辺が数値なら加算、両辺が文字列なら結合。型が食い違う場合(数値+文字列等)は実行時エラーになる(暗黙変換はしない。11節の`string()`で明示変換する)。他の算術演算子(`-` `*` `/` `%`)は両辺が数値でなければ同様に実行時エラーになる(文字列側の特別扱いは`+`だけ)。

**数値は常にfloat64相当のため、`0`除算はエラーにならず、Go/IEEE754の浮動小数点規則にそのまま従う。** `1 / 0`は`+Inf`、`-1 / 0`は`-Inf`、`0 / 0`は`NaN`になる(他の多くの言語のような「ゼロ除算エラー」は起きない)。`%`(剰余)も整数専用ではなく浮動小数点の剰余(`math.Mod`相当)として動くため、`5.5 % 2`のような小数同士の剰余も計算できる(`1.5`)。

**`==`/`!=`は、両辺の型が異なる場合は常に`false`/`true`を返す(エラーにはならない)。** `1 == "1"`は`false`。ただし**両辺がどちらもオブジェクト、またはどちらも関数(クロージャー)の場合、`==`/`!=`はコンパイラ・ランタイムのどちらでも防がれておらず、Goの実行時パニック(`comparing uncomparable type`)でプログラムがクラッシュする**——オブジェクト・関数はそもそも比較可能な値として設計されていない(19節)。数値・文字列・真偽値・`nil`同士の比較、および型が異なる値同士の比較(`nil == false`等、常に`false`)は問題なく動く。

```
1 == "1"          // false(型が違うだけ、エラーにはならない)
nil == false       // false
{} == {}           // 実行時パニックでクラッシュする(19節)
```

`<`/`<=`/`>`/`>=`は数値同士・文字列同士でのみ動く。文字列の順序比較はGoの文字列比較(バイト列としての辞書順)に従う。数値と文字列の比較、真偽値・`nil`・オブジェクト・関数を含む順序比較はいずれも実行時エラーになる。

## 9. メソッド呼び出しの糖衣構文(詳細)

`obj.method(a, b)`は次の展開の省略形。

```
obj.method(obj, a, b)
```

つまり`obj.method`をプロトタイプチェーンで検索した関数に対し、**第1引数として`obj`自身を自動的に差し込む**。これが「メソッド」らしい書き味を生んでいる。プロパティが関数でない場合、この形での呼び出しは実行時エラーになる。

```
alice.greet()
// 上は greet(alice) の糖衣構文(4.2節のgreetは fn(self) {...} という1引数関数だったため)
```

**この自己注入(`self`の自動差し込み)が起きるのは、`.method(...)`という形で直接呼び出した、最初の1回だけ。** `obj.method(a)(b)`のように戻り値をさらに呼び出す場合、2回目以降の`(b)`は通常のカリー適用であり、`self`は二重に差し込まれない。

```
alice.method(a)(b)
// = method(alice, a)(b)   -- self注入は最初の.method(...)呼び出しだけ
// (methodがa, self, bの3引数分をこの順で待つカリー関数であれば動く)
```

一方、`.method`というプロパティ自体をまず変数に代入してから呼ぶ場合は、この糖衣構文を経由しない普通の関数呼び出しになるため、`self`は自動的には渡らない——`self`を必要とする関数を`.`から切り離して呼ぶ場合は、自分で明示的に渡す必要がある。

```
greet = alice.greet   // ただのプロパティ読み取り(9節の糖衣構文の対象外)
greet(alice)            // selfを自分で渡す必要がある
```

## 10. スコープ

- スコープは**ブロック単位**。`{ }`(`if`/`while`/`for`/`main`/関数リテラルの本体)ごとに新しいスコープが作られる
- 内側のブロックで外側と同名の変数を宣言すること(シャドーイング)は許可する。ただし**「シャドーイング」と「既存の変数への再代入」は区別される**——同じ名前が既にどこかの外側スコープで見えている場合、内側での代入は新しい変数を作らず、その既存の変数を再代入する。どこにも見えていない名前への代入だけが、そのブロック内だけの新しい変数になる

```
x = 1
if true {
    x = 2   // シャドーイングではなく再代入(xは既に外側で見えている)
}
print(x)   // 2

if true {
    y = 3   // yはここで初めて見える名前 -> このifブロック内だけのローカル変数
}
print(y)   // コンパイルエラー: yは未定義(ifブロックの外からは見えない)
```

  この規則がないと、`while i < 10 { i = i + 1 }`のようなループのカウンタ更新が、ループの外の`i`ではなく毎回新しい`i`を作ってしまい、条件式の`i`が永遠に更新されない(無限ループになる)。「代入は、見えている既存の変数があればそれを再代入し、無ければ新規に作る」という規則は、この一番素朴な書き方が期待通りに動くために必要な規則である

- **`_`という名前は「書き込み専用の使い捨て」を表す予約された特別な名前**で、上記のシャドーイング・再代入の規則には従わない。`_ = value`(通常の代入)、`fn(_) {...}`(5節の引数、`fn() {...}`の展開形はこれになる)のどちらの形でも、値を束縛すること自体はできるが、**そのスコープの中で`_`を再び読み取ること・再代入することはできない**(コンパイルエラーになる)。「一度だけ書ける、その後は二度と触れない」という意味で、シャドーイングでも再代入でもない第三の扱いを受ける

```
_ = compute()   // 値を捨てる(副作用のためだけに呼ぶ、戻り値には興味が無い)
print(_)         // コンパイルエラー: "_"は読み取れない
_ = 2            // コンパイルエラー: "_"は再代入できない(既にこのスコープで束縛済み)

greet = fn(_) { return "hi" }   // 引数を無視するfn(_) {...}(fn() {...}の展開形)
```

  この「読み取り不可・再代入不可」の判定は**そのスコープ自身の中だけ**で行われる——外側のスコープで使われた`_`は、内側の(別の)スコープで新たに`_`を束縛することを妨げない(逆に、内側の`_`が外側の`_`を「再代入」したことにもならない)。関数リテラルの本体は常にそれ専用の新しいスコープなので、`fn(_) {...}`という引数は、その関数の外にある`_`と一切関係が無い

- 関数(カリー化された各段も含む)はレキシカルスコープを持ち、定義時点の外側の変数を**参照で**捕捉する(クロージャー)。値のコピーではなく、同じ変数そのものを見る——外側の変数がクロージャー生成後に変わっても、クロージャーの中からは変わった後の値が見える。逆にクロージャーの中から外側の変数を書き換えれば、外側からもその変更が見える。この性質を使うと、呼び出しをまたいで状態を蓄積するクロージャーが書ける

```
makeCounter = fn(start) {
    count = start
    return fn(step) {
        count = count + step
        return count
    }
}
counter = makeCounter(0)
counter(1)   // 1
counter(2)   // 3(前回の呼び出しの状態を覚えている)
counter(4)   // 7
```

- `name = fn(...) {...}`という形で関数リテラルを直接名前に代入した場合、その本体から`name`自身を呼び出せる(自己再帰)。`fn(n) { ... name(n - 1) ... }`という書き方が自然に動くのは、参照捕捉(直前の項目)により、クロージャー生成時点でまだ値が代入されていない`name`も、実際に呼び出される時点までに代入が完了していれば正しく解決されるため。ただし自己再帰に限る——2つの関数が互いを呼び合う相互再帰(`isEven`が`isOdd`を呼び、`isOdd`が`isEven`を呼ぶ)は非対応で、後で代入される側の名前は「まだ存在しない」として通常通りコンパイルエラーになる(19節)。**この自己再帰の仕組みは`name = fn(...) {...}`という直接代入の形にだけ働く**——オブジェクトリテラルのプロパティとして定義したメソッドが自分自身を再帰的に呼びたい場合(`{ fact: fn(self, n) { ... } }`のような形)は、この仕組みは使えない。代わりに9節の自己注入糖衣構文を使い、`self.fact(n - 1)`のように`self`経由で自分自身を呼び出す(こちらは既に`self`という通常の引数として渡ってきているので、無関係な別の仕組みで自然に動く)

```
fact = fn(n) {
    if n <= 1 { return 1 }
    return n * fact(n - 1)   // 直接代入された名前の自己再帰
}

counterProto = {
    count: 0,
    tick: fn(self) {
        self.count = self.count + 1
        if self.count < 3 { self.tick(self) }   // self経由の再帰(オブジェクトのメソッドはこちらの形を使う)
        return self.count
    }
}
```

- アクターに`spawn`される時点でオブジェクトは**コピー**される。以降、アクター内部の状態は元のオブジェクトとは独立する(コピー元を後から書き換えても、アクター側には影響しない)。**このコピーは浅い(shallow)コピー**——オブジェクト自身が持つプロパティの並び(トップレベルのキーと値)だけを複製し、プロパティの値がさらにオブジェクトの場合、そのネストしたオブジェクト自体は複製されず、コピー元とアクター側の両方から同じネストしたオブジェクトを参照する(そのネストしたオブジェクトを一方から書き換えると、他方からも見える)

## 11. 組み込み関数

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `print` | `(value) -> nil` | 値を標準出力へ書き、末尾に改行を付ける(数値・真偽値は自動で文字列化される)。**引数は常にちょうど1個**——複数の値をまとめて出力したい場合は`+`で文字列結合してから渡す |
| `string` | `(value) -> string` | 値を文字列へ変換する |
| `spawn` | `(obj) -> actor` | `obj`のコピー(浅いコピー、10節)を内部状態とするアクターを起動する(6.1節) |
| `send` | `(actor) -> fn(name, value) -> nil` | アクターへメッセージを投げる(6.2節)。ペイロードは常にちょうど1個の値(可変長引数ではない)。宛先が別のメッセージを処理中だと一時的にブロックされることがある(6.4節) |
| `ask` | `(actor) -> fn(name) -> value` | アクターへメッセージを送り、返信を待って受け取る(6.3節)。ユーザー自身のペイロードは渡せない(`replyTo`だけが自動で付く) |
| `reply` | `(replyTo, value) -> nil` | `ask`で送られたメッセージに応答する(6.3節) |
| `remove` | `(obj, name) -> nil` | オブジェクト自身から指定プロパティを削除する(親には影響しない) |
| `has` | `(obj, name) -> bool` | `obj`自身(プロトタイプは辿らない)が指定プロパティを持つか判定する |
| `list` | `(values...) -> obj` | 連番の数値キー(`0`,`1`,...)を持つオブジェクトを作る(3節)。`list()`(0個)は空リストになる |
| `len` | `(obj) -> int` | オブジェクトが自身に持つプロパティの数、または文字列の文字数(Unicodeの文字数であって、バイト数ではない)。オブジェクト・文字列以外(数値・真偽値・`nil`・関数)を渡すと実行時エラー |
| `at` | `(list, index) -> value` | `list`(3節の連番数値キーオブジェクト)の`index`番目(0始まり)の要素を読む。範囲外・非数値の`index`はその場でエラー(15.2節のGo資産呼び出し結果を読むのにも使う)。**負の添字は「末尾から数える」という特別扱いをせず、単に「存在しない添字」としてエラーになる**(Pythonの`list[-1]`のような負数インデックスは無い)。`list[index]`という角括弧の構文糖衣がある(3.1節)——書き込み(`list[index] = value`)はこの糖衣構文でのみできる |
| `raiseIfError` | `(value) -> nil` | `value`がGoの`error`型を実装する非`nil`値であればその場でエラーとして停止する。それ以外(`nil`・普通の値)は何もしない(15.2節のGo資産呼び出し結果のエラー確認に使う) |

**`list(...)`/`at(...)`には、要素の追加・削除・挿入・並べ替えといった配列操作が無い。** `remove(list, "0")`のようにプロパティとして削除すること自体はできるが、後続の要素が繰り上がる(インデックスが詰まる)ことはなく、単にその位置に穴が空くだけ——本格的な配列として使うには力不足で、あくまで「複数の値をひとまとめにして受け渡す」程度の用途を想定している(19節)。

## 12. エントリーポイント

```
main = fn(args) {
    print("Hello, Weave!")
    return 0
}
```

`main`はGoの構文キーワードではなく、`name = fn(param) {...}`という他のどんな関数定義とも見た目が全く同じ、予約された名前への代入として書く(専用の構文を増やさず`name = 予約された形(...)`というパターンで表現する、`gotype`/`gofunc`/`package(...)`と同じ流儀——`main`という名前自体は他の用途には使えない予約名で、この形以外で使うとコンパイルエラーになる)。5節の「Weaveの関数は常に1引数」というルールにmainも従い、その唯一の引数(名前は`args`である必要はない——何と名付けてもよい)には、プログラム自身の起動時に渡されたコマンドライン引数のlist(3節)が渡される。`args`の要素は常に文字列で、位置0にはプログラム自身の呼び出し名(Goの`os.Args[0]`相当)が入る。

```
main = fn(args) {
    print(len(args))       // 引数の個数(プログラム名を含む)
    print(at(args, 0))     // プログラム自身の呼び出し名
    return 0
}
```

戻り値は終了コード(数値でなければ実行時にエラーになる)。

コマンドライン引数に一切興味が無い場合は、5節の糖衣構文を使って`main = fn() {...}`と書いてもよい(`fn(_) {...}`と同じ意味になり、`args`は読み取り不可の`_`に束縛される)。

複数ファイル・複数パッケージで構成する場合、`main = fn(args) {...}`はコンパイル時にルートとして指定したパッケージにのみ、エントリーポイントとして働く(17.3節)。

## 13. キーワード一覧

```
fn true false nil
if elif else while for in break continue return
spawn send ask reply remove has list print string len at raiseIfError
gotype gofunc gomethod govar goReturns goParams package main
shape checkShape _
```

## 14. AMIVM-IRへの対応方針

**この節は実装着手前(仮説段階)の当初計画であり、実装を通して判明した制約により、最終的な実装は下記の方針と食い違っている箇所がある(特にオブジェクトの表現方法)。実際に採用された最終形は16節、および`weave_implementation_notes.md`・`CLAUDE.md`の「確定した設計判断」を参照——この節はあくまで「最初はこう考えていた」という記録として残している。**

- **オブジェクト**: ~~`MPTYPE ^string ^any`(キーは文字列、値は`any`)として表現する。`obj.x`の読み取りは`MGET`のcomma-ok形を使い、無ければ`__proto__`を辿って再帰的に`MGET`し直す(コード生成側のヘルパー関数として共通化する)~~ **(実装せず。Weaveの値が常に`any`であるため、AMIVMの`MPTYPE`/`MGET`は`^any`型の変数に対して直接使えないと判明し、結局オブジェクトはWeave独自のGoランタイム(`weavert.Object`)へ完全に一元化した——16節参照)**
- **カリー化**: 多引数に見える定義・呼び出しは、コンパイル時にすべて「1引数の`FNTYPE`+`CLOS`の連鎖」へ展開してから、通常のクロージャーとしてAMIVM-IRへ落とす。ランタイム側に特別な部分適用の仕組みは持たない(構文の見た目だけの問題であり、実体は素直なクロージャーの入れ子)
- **アクター**: ~~`spawn`は`CHTYPE`(メッセージ型のチャネル)+`CHMAKE`+`SPAWN`(受信ループを回すgoroutine)の組み合わせ。メッセージは`STTYPE`で`{ name: string, args: object, replyTo: chan<any> }`のような構造体として表現し、`CHSEND`/`CHRECV`でやり取りする~~ **(実装せず。オブジェクトと同じ理由でAMIVMのネイティブなチャネル/構造体命令は使わず、Goのgoroutine・channelを`weavert`内に完全に隠蔽する設計にした。IR側は`spawn`/`send`/`ask`/`reply`をweavertへのCALLとして生成するだけ)**。受信ループの中で「`self`オブジェクトから`msg.name`と同名のプロパティを検索して呼ぶ」処理は、オブジェクトのメソッド呼び出し(9節)と共通のヘルパー関数を使う、という部分だけは計画通り実現された(0節の統一原理がコード生成レベルでもそのまま活きる)
- **静的型付けとのハイブリッド**: 変数の型が実行時まで分からない(動的型付け)ため、素朴には全ての演算をGoの`interface{}`(`any`)越しに行う必要がある。ただし`x = 1; y = 2; z = x + y`のように**コンパイラが両辺の型をリテラル・直前の代入から静的に確定できる場合**は、AMIVMの`ADD`命令をそのまま数値型に対して使い、確定できない場合(プロパティ経由で得た値同士の演算など)だけ、小さな付属Goランタイム(`weavert`のようなパッケージ名を想定)の`?weavert.Add(a, b) any`のような関数呼び出しへフォールバックする。これは「動的型付けの自由さ」と「AMIVMのネイティブ演算命令の速さ・単純さ」を両立させるための設計判断で、Seed/Cascadeが確立した「専用ランタイムパッケージを`go:embed`で埋め込む」パターンをそのまま流用できる見込み

## 15. Go資産の利用

WeaveはGoの構造体・関数を**宣言済みのものだけ**扱える(4節のプロトタイプオブジェクトとは違い、Go側の値は実行時にフィールドを追加できないため、動的な仕組みとは別扱いにする)。

**Go関数・Goメソッドの呼び出しは、戻り値の個数によらず常に3節のlist(連番の数値キーを持つオブジェクト)を返す。** Weaveには複数戻り値という概念が無いため(現時点でこのコンテナ内でのみ使われる言語であることを踏まえ、後方互換を気にせず設計をやり直した結果の判断——過去には「戻り値1個ならスカラー、`(値, error)`だけ特別扱い」という非対称な設計だったが、常にlistへ統一した)、`at(list, index)`(11節)で個々の値を読み出す。

### 15.1 Go型の宣言

```
GoFile = gotype("?os.File", {
    Close: gomethod("Close"),
    Name: gomethod("Name")
})
```

`gotype(型名, メンバー定義)`は、指定したGo型(`os.File`)の**メソッドテーブル**を表すプロトタイプオブジェクトを作る。`gomethod(名前)`は、Go側の実際のメソッド名(大文字始まりの公開メソッド)をWeave側から呼べるようにする印。

### 15.2 Go関数の宣言と呼び出し

```
GoFile = gotype("?os.File", { Close: gomethod("Close") })
goOpen = gofunc("?os.Open", goReturns(GoFile, "?error"), goParams("?string"))

result = goOpen("data.txt")   // 常にlistが返る
f = at(result, 0)              // 位置0はGoFileプロトタイプが自動で結びつく
raiseIfError(at(result, 1))    // 位置1はGoのerror(15.4節)
f.Close()                       // 普通のメソッド呼び出しと同じ書き方
```

`gofunc(関数の完全名, goReturns(...), goParams(...))`は、Go関数をWeaveから呼べる普通の関数値にする。呼び出し結果は常にlistで、各位置の値は宣言した`goReturns(...)`の並びに対応する(数値・文字列・真偽値はそのまま、`gotype`宣言を渡した位置はその値に自動でプロトタイプが結びつく——15.4節で詳しく説明する型ヒント構文がこの宣言の実体)。

### 15.3 宣言はモジュールの入口で一度だけ

`gotype`/`gofunc`による宣言は、通常はプログラム冒頭(あるいは専用の宣言ファイル)にまとめて書く。一度宣言してしまえば、それ以降のコードは4節のプロトタイプオブジェクトと15.2節のGo資産を、`at(...)`で取り出した後は**区別せず同じ`.method()`という書き方**で扱える。

```
counterProto = { increment: fn(self, n) { self.count = self.count + n } }
// ↑ 純粋なWeaveオブジェクトの定義

GoFile = gotype("?os.File", { Close: gomethod("Close") })
goOpen = gofunc("?os.Open", goReturns(GoFile, "?error"), goParams("?string"))
// ↑ Go資産の宣言

// これ以降、どちらも同じ .method() の書き方で呼べる(Go資産側はat(...)で
// listから取り出した後)
```

### 15.4 型ヒント —— `goReturns(...)`/`goParams(...)`

`gotype`/`gomethod`/`gofunc`は、Goの型をそのまま書いた文字列を渡すことで、呼び出し時の型を宣言する。**`gomethod`/`gofunc`は「型ヒントを一切書かない(常に未型付き・reflect経由)」か「戻り値・引数のどちらも型を宣言する(常に型付き・完全ネイティブ)」のどちらか一方でなければならない**——一部だけ型を書く中間状態は無い(all-or-nothing)。型を書いた場合は、実行時に実際の値がその型と一致するかを厳密に検証し、一致しなければその場で分かりやすいエラーで止まる(TypeScriptに対するJavaScript、Pythonの型ヒントと同じ位置づけで、実行速度のための機能ではない——安全性・保守性のための機能)。

```
GoReader = gotype("?*strings.Reader", {                       // レシーバーの型(ポインタなら*を書く)
    len: gomethod("Len", goReturns("?int"), goParams())         // 戻り値1個・引数0個
})
newReader = gofunc("?strings.NewReader", goReturns(GoReader), goParams("?string"))

result = newReader("hello")
r = at(result, 0)
n = at(r.len(), 0)   // r.len()自身もlistを返す(戻り値1個でもlist化される)
```

- `gotype`の第1引数は、`*`を付けるとポインタ型、付けなければ値型として扱われる(実際にGoの値がどちらで保持されているかに正確に合わせる必要がある——多くのGo標準ライブラリの型はポインタで返ってくる)
- `gomethod(名前, goReturns(...), goParams(...))`/`gofunc(関数名, goReturns(...), goParams(...))`——型を宣言する場合は必ずこの3引数の形。`goReturns(...)`はそのGo呼び出しの戻り値の並びを宣言する(0個以上、実際のGoの戻り値の個数と一致させる)。各要素は`"?pkg.Type"`という型文字列、または`gotype(...)`で宣言済みの名前(その位置がGo構造体/ポインタで、自動的にそのプロトタイプと結びつく——旧設計の「戻り値のプロトタイプ」引数はここへ統合された)のどちらか。`goParams(...)`は引数の型を宣言する(0個以上)
- 型が一致しなかった場合、実行時にその場でエラーになり、Weave側で意味の分かるメッセージ(どこで・何を期待して・実際は何だったか)を伴う
- **エラー確認は型宣言と切り離されている**——`goReturns(...)`はあくまで型を宣言するだけで、Goの`(値, error)`という形を特別扱いして自動でエラーチェック・停止することはしない。`raiseIfError(at(result, N))`のように、呼び出し側が明示的にエラー位置を取り出して確認する(11節)
- **スライス型ヒントは`"?[]byte"`と、`int`/`int8`/`int16`/`int32`/`int64`/`uint`/`uint16`/`uint32`/`uint64`/`float32`/`float64`/`string`/`bool`を要素型とする形(`"?[]int"`・`"?[]string"`等)をサポートする。** `goReturns(...)`/`goParams(...)`のどちらの位置にも書ける。`"?[]byte"`(`"?[]uint8"`も同義)だけは特別扱いで、実際のGoの`[]byte`をWeaveの文字列へ自動変換する(`string(...)`と同じ変換、15.1〜15.3節の型ヒント無し経路が既に行っている変換と同じ結果になる)。それ以外の要素型は、Weaveの**list**(3節)へ自動変換する——戻り値側ならGoのスライスを1要素ずつ変換したWeaveのlistに、引数側ならWeaveのlistを1要素ずつ検証しながら変換したGoのスライスになる。構造体・ポインタ・他のスライスを要素型とするスライス(`"?[][]int"`・`"?[]*os.File"`等)には対応する値表現が無いため、型ヒントとして書けない(コンパイルエラー)

```
readFile = gofunc("?os.ReadFile", goReturns("?[]byte", "?error"), goParams("?string"))
result = readFile("data.bin")
raiseIfError(at(result, 1))
print(at(result, 0))   // Goの[]byteが自動でWeaveの文字列になる

GoBytesReader = gotype("?*bytes.Reader", { len: gomethod("Len", goReturns("?int"), goParams()) })
newReader = gofunc("?bytes.NewReader", goReturns(GoBytesReader), goParams("?[]byte"))
newReader("raw bytes")   // Weaveの文字列が自動でGoの[]byteになる

fields = gofunc("?strings.Fields", goReturns("?[]string"), goParams("?string"))
fieldList = at(fields("the quick brown fox"), 0)   // Weaveのlistになる("the","quick","brown","fox")
print(fieldList[0])                                  // "the"(3.1節の添字構文)

join = gofunc("?strings.Join", goReturns("?string"), goParams("?[]string", "?string"))
print(at(join(fieldList, "-"), 0))                   // "the-quick-brown-fox"(逆方向の変換)
```

`gomethod`の戻り値・引数にも同じスライス型ヒントを書ける(内部的にamivmの`METHOD`命令を経由するため、`gotype`/`gofunc`と全く同じ扱いになる——16節参照)。

**数値の型ヒント(`"?int"`・`"?byte"`・`"?uint64"`等)は`goReturns(...)`・`goParams(...)`のどちらの位置にも書ける。** Weaveの数値は常に`float64`(2節)であり、`goParams(...)`側は「Weaveの値を実際にASSERTで検証してから、宣言されたGoの数値型へ変換する」という順序で処理する——先に実際の動的型である`^float64`へASSERTし(ここで初めて型の不一致がチェックされる)、それから`int(...)`のようなGoのネイティブな数値変換を行う。戻り値側(`goReturns(...)`)は元から同じ理屈で`float64`へ変換していた(15.1節以降)ので、これは引数側にも対称な扱いを及ぼしただけ——どちらの向きでも完全にネイティブ(reflect不使用)。

```
repeat = gofunc("?strings.Repeat", goReturns("?string"), goParams("?string", "?int"))
print(at(repeat("ab", 3), 0))   // "ababab"
```

### 15.5 Go変数の読み取り —— `govar(...)`

```
Stdout = govar("?os.Stdout")
ErrRange = govar("?strconv.ErrRange")
```

`govar(変数の完全名)`は、Goの**パッケージレベル変数(または定数)**を、`gotype`/`gofunc`(15.1〜15.2節)と全く同じ「コンパイル時にのみ意味を持つ、予約された形」の宣言として読み取れるようにする。第1引数は`gotype`/`gofunc`と同じ`"?pkg.Name"`という形の文字列(先頭の`?`必須)。

`govar`で宣言した名前を式の中で参照するたびに、その時点の**実際のGo変数の現在値**をそのつど読み直す——宣言した瞬間の1回のスナップショットではない、という意味で「ライブ」な参照になる。

```
Stdout = govar("?os.Stdout")
writeString = gofunc("?io.WriteString")
result = writeString(Stdout, "hello\n")   // os.Stdoutへ直接書き込む
raiseIfError(at(result, 1))
```

- **読み取り専用。** `govar`で宣言した名前へ通常の代入(`Stdout = 何か`)をしても、実際のGoパッケージ変数への書き込みにはならない——ただのWeaveローカル変数への再代入になり、以後その名前は`govar`との結びつきを失う(17.4節の`package(...)`修飾子の再代入と同じ「静かに外れる」扱い)
- **govarで読んだ値に対して直接`.method(...)`という書き方はできない。** 9節の自己注入糖衣構文はWeave自身のオブジェクト(3節のmap表現)を前提にしており、Go資産由来の生の値には使えない(実行時に分かりやすいエラーになる——「value is not an object」)。Go資産のメソッドを呼びたい場合は、これまで通り15.1節の`gotype`(`gomethod`によるメソッドテーブル)を使うか、`govar`で読んだ値を15.2節の`gofunc`の引数としてGo関数へ渡す(上の`io.WriteString`の例)

### 15.6 Go資産利用の制約

- `gotype`/`gofunc`/`govar`の第1引数(Go資産の完全名)には、**Go標準ライブラリ(`strings`・`os`・`strconv`等)、またはWeaveのビルド成果物自身が既に参照している他のパッケージの関数・メソッド・変数・型でなければ**、確実に解決できるとは限らない。任意のサードパーティ製Goパッケージを`"?github.com/someone/somepkg.Func"`のように書いても、`go build`の依存解決の都合で失敗しうる(19節)
- `gotype`/`gofunc`で扱えるのはGoの**関数・メソッド**だけで、Goの型を新しく構築すること(Goの構造体リテラルをWeave側から組み立てる)はできない——常に「既存のGo関数・メソッドを呼んで返ってきた値を受け渡す」という形でしかGoの値に触れられない(19節)。パッケージレベル変数の**読み取り**は15.5節の`govar`で可能になったが、書き込みはできない
- `goReturns(...)`/`goParams(...)`(15.4節)に書ける型は、AMIVM-IRが直接表現できる型トークンの形に限られる。**スライス型ヒントは要素型がスカラー(数値・文字列・真偽値)の場合だけサポートする**(15.4節)——構造体・ポインタ・他のスライスを要素型とするスライス(`"?[][]int"`・`"?[]*os.File"`等)は書けず、そのようなスライスを返す・受け取るGo関数は型ヒント無し(reflect経由、15.1〜15.3節)の形でしか使えない(19節)
- 呼び出したGo関数・メソッドが(戻り値としてエラー値を返すのではなく)Go自身の`panic`を起こした場合、Weave側で捕まえる方法は無く、Weaveプログラム全体が停止する(6.5節のアクターハンドラ内のエラーと同じ扱い)

## 16. AMIVM-IRへの対応(Go資産の利用について)

- `gotype`/`gofunc`はコンパイル時に評価される宣言であり、実行時のオブジェクトではない。コンパイラはこれらの宣言を見た時点で「この識別子(`GoFile`, `goOpen`)は動的なプロトタイプオブジェクトではなく、静的に確定したGo型・Go関数への参照である」と記録する
- **型ヒント無し(15.1〜15.3節)の`gofunc`/`gomethod`呼び出しはreflect経由で実際のGo呼び出しを行い、戻り値の個数によらずその場で全ての戻り値を集めてWeaveのlistとして組み立てる。** `(値, error)`という形を特別扱いする判定ロジックは存在しない——常にlistという設計そのものが、個数で場合分けする必要を無くしている
- **15.4節の型ヒントを書いた場合、その呼び出しはreflectすら経由しない完全にネイティブな命令列(型アサーション+ネイティブなメソッド値取得+ネイティブな複数代入`CALL`)に展開される。** `goReturns(...)`の宣言した個数だけAMIVMの`CALL`に結果代入先を並べる(AMIVMは元から複数の戻り値を1つの`CALL`で受け取れる)——0個・1個・2個以上のいずれも同じ仕組みで扱う。各結果は`weavert.NewObject`+`weavert.ObjSet`でlistへ詰め直され、`gotype`宣言と結びついた位置は結びつけの情報がコンパイル時にだけ追跡される(実行時の値自体はGoの生の値のまま、ラッパーオブジェクトは作らない)。型が一致しない場合はASSERTの時点で検出され、分かりやすいエラーになる
- **`gomethod`の型ヒント付き呼び出しは、メソッド値の取得にAMIVMの`METHOD`命令(`local := variable.method`)を使う。** 以前はGoの構造体フィールド取得と同じ`FGET`(既存のVARへの代入)を流用していたが、これは名前付きスライス型のような型ヒントと根本的に相性が悪かった——`FGET`の代入はGoの関数型の**完全な型の同一性**を要求し、`^GoBytes`のような名前付きの代役型は、基底型が同じでも実際のメソッドの型(`func(...) []byte`)とは別の型として扱われ、コンパイルエラーになる(直接probeで確認済み)。`METHOD`は`:=`で型推論しながら宣言するため、この問題が起きない——以降の「引数を渡す」「戻り値を代入する」はどちらもGoの代入可能性(名前付き型・無名型で基底型が同じなら、片方が無名なら変換なしで通る)のルールで済むため、名前付きの代役型のままで問題なく動く
- **スライス型ヒント(15.4節)は要素型によって異なる変換経路を通る。** `"?[]byte"`(`"?[]uint8"`)は`string(...)`という単純なネイティブ型変換で済むが、その他の要素型(`int`・`string`・`bool`等)はGoのスライスを1要素ずつ辿ってWeaveのlistへ詰め直す(逆方向も同様)必要があり、単純な型変換では足りない。この反復処理は`weavert`の専用関数(`weavert.IntsToList`/`weavert.ListToInts`のような、要素型ごとに用意された関数)が担う——ただしこれは**要素型がコンパイル時に静的に確定しているため型ごとに専用の関数を選ぶだけ**であり、reflectによる実行時の型判定は一切行わない(型ヒント無し経路の`weavert.NormalizeGoValue`とは異なる)
- **`govar`(15.5節)は`gotype`/`gofunc`よりも単純に、常にネイティブな命令列で済む。** `"?pkg.Name"`という文字列はそれ自体が有効なAMIVMの`value`オペランド(生のGo識別子参照)であり、参照するたびにそのままCALLの引数として埋め込まれる——名前解決の対象が「動的に検索するメソッド名の文字列」ではなく「コンパイル時に確定した識別子そのもの」であるため、reflectを経由する理由がそもそも無い(15.1〜15.3節の型ヒント無し経路とは違い、typed/untypedの使い分けも存在しない)。この「参照のたびに新しいCALLを生成する」という性質そのものが15.5節の「ライブ」な読み取りを実現している——宣言時に1回だけ評価してどこかにキャッシュする、という設計ではない

## 17. モジュールと複数ファイルの統合

`gotype`/`gofunc`(15〜16節)と同じく、実行時の動的な仕組み(0節の統一原理)を経由しない、コンパイル時に完結する静的な機構。

### 17.1 パッケージ = ディレクトリ

同じディレクトリに置かれた`.weave`ファイル群は、1つの「パッケージ」として1つのプログラムへ統合される。パッケージ**内**のファイル間では17.2節の`package(...)`は不要——全てのファイルが1つの共有スコープ(10節)にあるかのようにコンパイルされる。ファイルをどう分割するかは開発者の整理都合であり、言語仕様上の意味は持たない。

```
myproject/
    main.weave
    reading.weave
```

上記2ファイルは`myproject`という1つのパッケージとして一括コンパイルされ、`main.weave`から`reading.weave`で定義した名前をそのまま(`package(...)`なしで)参照できる。

コンパイル対象として**単一ファイル**を直接指定した場合は、そのファイル1つだけの独立したパッケージとして扱う(同じディレクトリに他の`.weave`ファイルがあっても無視する)。`examples/`以下のように、同じディレクトリに互いに無関係な複数のプログラムを置く運用と両立させるための実務上の配慮であり、モジュール機構導入前の既存プログラムが無変更で動き続けることを意図している。

### 17.2 パッケージをまたぐ参照 —— `package(...)`と大文字始まり公開

別のディレクトリにあるパッケージを参照したい場合は、`gotype`/`gofunc`(15節)と全く同じ形——専用の構文やキーワードを新設せず、`名前 = package("<相対パス>")`という普通のトップレベルの束縛として書く。

```
mathutil = package("./mathutil")

main = fn(args) {
    print(string(mathutil.Clamp(15, 0, 10)))
    return 0
}
```

`package("<相対パス>")`(パスは文字列リテラルのみ——コンパイル時に解決するため、計算式は書けない)は、指定したディレクトリ(または17.6節の`.wvz`アーカイブ)のパッケージを、代入先の名前を修飾子としてこのファイルに束縛する。パスはこのファイル自身のディレクトリからの相対パス。トップレベルであればファイル中のどこに書いてもよい。

他パッケージから参照できるのは、**名前が大文字で始まるトップレベルの束縛だけ**(Goの可視性規則と同じ)。`pub`のような別建ての修飾子は無く、束縛自身の綴りがそのまま可視性を表す。

```
// mathutil/clamp.weave
Clamp = fn(v, lo, hi) {
    if v < lo { return lo }
    if v > hi { return hi }
    return v
}

helper = fn(x) { return x }   // 小文字始まりなので他パッケージから見えない
```

値の種類は問わない——オブジェクト・関数・スカラー・`gotype`/`gofunc`宣言(15節)のいずれも、大文字で始めれば公開される。

**`修飾子.名前`という参照は、4節・9節の通常のプロパティアクセス/メソッド呼び出し糖衣構文とは別物であり、`self`の自動注入(9節)は一切起きない。** `mathutil.Clamp(15, 0, 10)`は`Clamp(15, 0, 10)`という素直な(1引数関数3連鎖の)呼び出しに解決され、`Clamp(mathutil, 15, 0, 10)`のようにはならない——`mathutil`はコンパイル時にのみ意味を持つ名前であり、実行時の値・オブジェクトではないため(17.4節参照。15節の`gotype`/`gofunc`が「動的なプロトタイプオブジェクトではなく静的な参照」であるのと同じ立ち位置)。

`package(...)`で導入した修飾子と同じ名前を、そのファイル内で別の値に再代入した場合は、その時点で修飾子としての結びつきを静かに失う(`gofunc`宣言済みの変数を別の値へ再代入した場合、15〜16節の静的なGo型付けが黙って外れるのと同じ扱い)——コンパイルエラーにはしない。

### 17.3 `main`はルートパッケージでのみエントリーポイントになる

`main = fn(args) {...}`(12節)は、コンパイル時にルートとして指定したパッケージ(通常はプロジェクト最上位のディレクトリ、直接指定した単一ファイル、または17.6節の`.wvz`アーカイブ)に書いたものだけが実際のエントリーポイントとして扱われる。`package(...)`で取り込まれている側(非ルートパッケージ)が偶然`main`という名前をトップレベルで使っていても、コンパイルエラーにはしない——17.4節の機械的なリネームがそのまま適用され、他の名前と同じように`パッケージ名_main`という私的な束縛になるだけで、エントリーポイントとしては一切扱われない(取り込む側から`パッケージ名.main`のように参照することもできる、ただの1関数として)。

### 17.4 コンパイル時の名前解決

`gotype`/`gofunc`(15〜16節)と同様、`package(...)`はコンパイル時にのみ意味を持つ。コンパイラは複数ファイル・複数パッケージを、実行が始まる前に**1つのプログラムへ静的に統合**する——修飾子付き参照(`mathutil.Clamp`)は対象パッケージの束縛へコンパイル時に直接解決され、実行時に名前で検索する仕組み(4.2節のプロトタイプチェーン)は一切経由しない。

この統合の過程で、非ルートパッケージのトップレベル束縛(公開・非公開を問わない——非公開の名前も、同じパッケージ内の公開された束縛から参照される可能性があるため、プログラムには組み込まれる)は全て、パッケージ名を使って一意な名前へ機械的に置き換えられる(例: `mathutil`パッケージの`Clamp`は内部的に`mathutil_Clamp`のような名前になる)。ユーザーがこの置き換え後の名前を直接書く必要はなく、修飾子付き参照は常にコンパイラが自動的に解決する。**ルートパッケージ自身の束縛には、この名前の書き換えを行わない**(ルートパッケージが1つに定まるため、一意化の必要が無い)。

`package(...)`で取り込まれたパッケージの束縛(と、そのトップレベル文が持つ副作用)は、取り込む側自身のトップレベル文・`main`本体よりも**先に**評価される。あるパッケージが複数箇所から取り込まれていても、そのパッケージの評価は1回だけ行われる。

**この機械的な名前の置き換えは、パッケージの完全パスではなく、ディレクトリ名(ベースネーム)だけを使う。** そのため、**別々の場所にある2つのパッケージが偶然同じディレクトリ名を持っている場合(例: `foo/utils`と`bar/utils`)、両方を同じプログラムに取り込むと、置き換え後の名前(`utils_X`)が衝突し、片方の束縛がもう片方を静かに上書きしてしまう**(コンパイルエラーにはならず、実行時に意図しない方の値が使われる)。現状この衝突を検出する仕組みは無いため(19節)、取り込むパッケージ同士のディレクトリ名が重ならないよう、開発者自身が気をつける必要がある。

### 17.5 循環参照の禁止

パッケージAがパッケージBを`package(...)`で取り込み、BもAを直接・間接に取り込んでいる場合はコンパイルエラーとする(依存関係は有向非巡回グラフでなければならない)。

### 17.6 `.wvz`アーカイブ

パッケージのソースは、実ディレクトリの代わりに1つの`.wvz`ファイル(ディレクトリをZIP圧縮しただけのアーカイブ、拡張子のみが独自)としてまとめて配布できる。`package(...)`の引数がディレクトリを指していても`.wvz`ファイルを指していても、書き手からは区別がない——どちらも同じ`.weave`ファイル群として読み込まれる。

```
mathutil = package("./mathutil.wvz")   // ディレクトリの代わりに.wvzを指す以外は17.2節と同じ
```

コンパイル対象(ビルドのルート)を`.wvz`で指定すること自体は可能で(`weave build`/`run`/`emit-ir`/`emit-go`はディレクトリと`.wvz`を区別しない)、その場合は17.1節の「ディレクトリをルートに指定する」場合と同じ意味になる。ただし下記の`weave wvz`サブコマンド自身は、そのような「自分自身のエントリーポイントを持つ実行可能プログラム」の`.wvz`化はサポートしない(後述)——`.wvz`はあくまで`package(...)`で取り込む**パッケージ**の配布形式であり、実行可能プログラムの配布は`weave build`の役割と重複するため。

`.wvz`アーカイブの作成は、`weave`コマンド自身の`wvz`サブコマンドで行う。

```
weave wvz ./mathutil            # ./mathutil.wvz を生成
weave wvz -o out.wvz ./mathutil # 出力先を明示
```

`weave wvz <ディレクトリ>`は、指定ディレクトリ**直下**の`.weave`ファイルだけをアーカイブする(サブディレクトリは含めない——17.1節の「パッケージ=1ディレクトリ」という単位とそのまま対応させるため)。

対象ディレクトリが(それ自身をルートとして読み込んだ場合に)自分自身のエントリーポイント(`main = fn(args) {...}`)を持つ場合はコンパイルエラーとする——`.wvz`化する対象は常に「`package(...)`で取り込まれる側」であり、自分自身のエントリーポイントを持つ実行可能プログラムを配布したい場合は`weave build`/`weave run`を使うべきだからである(17.3節が定める「非ルートパッケージの`main`は単なる私的束縛になる」という扱いとは別の話——ここでの判定は`.wvz`化しようとしているディレクトリ自身を*ルートとして*読み込んだ結果を見ている)。

アーカイブする前に、そのディレクトリのソースが実際に(`go build`まで通して)ビルドできることを確認し、ビルド成果物は破棄した上でアーカイブに進む——壊れたソースを圧縮してしまうことを防ぐため。対象ディレクトリは(上記の通り)エントリーポイントを持たないため、ビルド確認のためだけに`main = fn(args) { return 0 }`という仮の入口を一時的に補って検証し、これも成果物と一緒に破棄する。

## 18. まとめのサンプルプログラム(Go資産利用版)

0節で述べた統一原理(プロトタイプ検索=メッセージディスパッチ)に加え、15〜16節のGo資産利用も1つのプログラムで確認する例。「ファイルを読むアクター」が、Go標準ライブラリの`os.File`をWeaveオブジェクトと同じ書き味で扱う。

```
// --- Go資産の宣言(冒頭でまとめて行う) ---
GoFile = gotype("?os.File", {
    Close: gomethod("Close", goReturns("?error"), goParams())
})
goOpen = gofunc("?os.Open", goReturns(GoFile, "?error"), goParams("?string"))

// --- Weave自身のプロトタイプ ---
readerProto = {
    open: fn(self, path) {
        result = goOpen(path)
        raiseIfError(at(result, 1))
        self.file = at(result, 0)     // Go資産をプロパティとして保持
    },
    finish: fn(self, replyTo) {
        raiseIfError(at(self.file.Close(), 0))   // Go構造体のメソッドを普通の.呼び出しで
        reply(replyTo, true)
    }
}

main = fn(args) {
    a = spawn({ __proto__: readerProto, file: nil })
    send(a)("open", "data.txt")
    done = ask(a)("finish")
    print(string(done))     // "true"

    return 0
}
```

`a`(アクター)の内部状態に保持された`self.file`は、`gotype`で宣言した`GoFile`をプロトタイプに持つ**Go資産由来のオブジェクト**だが、Weave自身のプロトタイプオブジェクトと**全く区別せずに**扱えている(`self.file.Close()`という書き方は、4節の純粋なWeaveオブジェクトへのメソッド呼び出しと同一の構文)。アクター・カリー化・プロトタイプOOP・Go資産利用という4つの要素が、この短い例の中に全て収まっている。

## 19. 現状の仕様でできないこと

このリストは「まだ実装されていないだけで、いずれ対応する予定」という機能一覧ではない——現時点のWeaveの設計上、**構文として書けない・意味論として存在しない**ものを、読者が仕様書を読み返しても答えが見つからずに困らないよう明示的に列挙したもの。ここに挙がっている項目について、他の節を探しても追加の情報は無い。

### 19.1 構文

- **識別子はASCII文字のみ**(1.0節)。変数名・関数名・プロパティ名にUnicode文字(`名前`のような)は使えない(文字列リテラルの中身は自由)
- **数値リテラルに指数表記(`1e10`)・16進/8進/2進表記(`0x1F`)・先頭が`.`で始まる小数(`.5`)は無い**(1.0節)
- **角括弧`[ ]`は`list[index]`という添字の読み書き専用で、それ以外の用途には使えない**(3.1節)。配列リテラル(`[1, 2, 3]`)は書けない——配列相当は`list(...)`で作る(3節)。`obj[key]`のような、計算した文字列で任意のプロパティへアクセスする構文(計算されたプロパティアクセス)は依然として存在しない(4.1節)——`list[index]`は数値インデックス専用で、文字列キーには使えない
- **オブジェクトリテラルのキーは識別子のみ**——文字列キー(`{"a-b": 1}`)や計算キー(`{[expr]: 1}`)は書けない(3節)
- **複合代入・インクリメント演算子(`+=`・`++`等)は無い**。`i = i + 1`と書く(8節)
- **三項演算子(`cond ? a : b`)・`switch`/`case`文は無い**。`if`/`elif`/`else`だけを使う(7節)
- **分割代入(destructuring、`{x, y} = obj`のような)は無い**。プロパティを1つずつ読み出す

### 19.2 値・型・演算子

- **静的型付け・型注釈・ジェネリクスは無い**(常に動的型付け、2節)。`gotype`/`shape`の型ヒント(4.3節・15.4節)はあくまで実行時の値の検証であり、コンパイル時の静的型検査ではない
- **整数と浮動小数点数の区別が無い**(常にfloat64相当、2節)
- **オブジェクト同士・関数(クロージャー)同士を`==`/`!=`で比較すると、実行時にGoの生のパニック(`comparing uncomparable type`)でプログラム全体がクラッシュする**(8節)。オブジェクト・関数はそもそも比較可能な値として設計されていない——回避策は無い(構造的な等価性判定が必要な場合は、比較したいプロパティを自分で1つずつ比較する)
- **条件式のtruthy/falsy変換が無い**——`if`/`while`/`&&`/`||`は実際の真偽値でなければ実行時エラーになる(2節)
- **例外処理(`try`/`catch`、投げて捕まえる例外オブジェクト)が無い。** エラーは実行時パニックとしてプログラム全体を即座に停止させる形でしか起こらず(6.5節・15.6節)、パニックを「捕まえて」処理を続ける仕組みは無い。エラーを値として扱いたい場合は、Go資産の戻り値(15節)のように「エラーかもしれない値」を明示的な戻り値として設計し、`raiseIfError`を呼ぶかどうかを呼び出し側が選ぶ、という形にする
- **リフレクション・実行時型問い合わせ(`typeof(x)`のような組み込み)は無い。** `shape`/`checkShape`(4.3節)は「期待通りかどうか」を検証するだけで、値の種類を調べて分岐する手段そのものではない

### 19.3 オブジェクト・プロトタイプ

- **計算されたプロパティアクセス(`obj[name]`)は無い**(4.1節、19.1節と同じ制約の言い換え)。プロパティ名は常にソースコード上の識別子として書く
- **プロトタイプチェーンの循環検出は無い**(4.2節)。循環を作ってしまうと`obj.x`のようなプロパティ読み取りが無限ループする
- **`shape`は1階層のみで、ネストしたオブジェクトを再帰的に検証しない。「このプロパティは省略可能」という宣言(オプショナルなプロパティ)も無い**(4.3節)——宣言した全プロパティが必須項目として扱われる

### 19.4 関数

- **相互再帰(2つの関数が互いを呼び合う)は無い。直接の自己再帰(`name = fn(...) { ... name(...) ... }`)のみ対応**(10節)
- **`_`という名前は読み取り不可・再代入不可の特別な予約名で、通常の変数として使うことはできない**(10節)。`_`という名前で値を書けるのは各スコープにつき1回だけで、その後は二度と読んだり書いたりできない

### 19.5 アクターモデル

- **アクター1つがクラッシュしても他が生き残る、という部分的な障害分離(フォールトトレランス)は無い。** メッセージハンドラの実行中に実行時エラーが起きると、そのアクターだけでなく**プログラム全体**が即座に異常終了する(6.5節)
- **`send`/`ask`のペイロードは可変長引数ではない。** `send`は常にメッセージ名+ちょうど1個の値、`ask`はメッセージ名のみ(ユーザー自身のペイロードは渡せない)(6.2節・6.3節)
- **アクターの受信キューにバッファは無い。** `send`は宛先アクターが別のメッセージを処理中だと一時的にブロックされることがある(6.4節)——常に瞬時に返る「完全な非同期」ではない

### 19.6 制御構文

- **カウンタ・範囲形の`for`(`for i = 0; i < 10; i++`のような)は無い。** `for`は`for k, v in obj`(オブジェクトの自身のプロパティ列挙)専用で、まだ存在しない数値の範囲を一から生成するには`while`+カウンタ変数を自分で書く(7節)。**既存の`list(...)`を順に処理するだけなら`for k, v in list(...)`で足りる**(3.1節・7節)——`while`が必要になるのは「範囲そのものをまだ持っていない」場合に限られる

### 19.7 Go資産連携(15〜16節)

- **`govar`(15.5節)によるGoパッケージレベル変数へのアクセスは読み取り専用。** Weave側から実際のGo変数へ書き込む(代入する)ことはできない
- **`govar`で読んだ値に直接`.method(...)`という書き方はできない。** 9節の自己注入糖衣構文はWeave自身のオブジェクトを前提にしているため——`gotype`のメソッドテーブル経由か、`gofunc`の引数として渡す形を使う(15.5節)
- **Weave側からGoの構造体リテラルを新しく組み立てることはできない。** 既存のGo関数・メソッドが返してきた値を受け渡すことしかできない(15.6節)
- **Go標準ライブラリ、または既に参照されている他のパッケージ以外(任意のサードパーティ製Goパッケージ)の関数・メソッド・変数は、確実に解決できるとは限らない**(15.6節)
- **スライス型ヒントは要素型がスカラー(数値・文字列・真偽値)の場合だけサポートする。** 構造体・ポインタ・他のスライスを要素型とするスライス(`"?[][]int"`・`"?[]*os.File"`等)は`goReturns(...)`/`goParams(...)`に書けず、そのようなスライスを返す・受け取るGo関数は型ヒント無しの経路でしか使えない(15.4節・15.6節)
- **Go関数・メソッドが(エラー値を返すのではなく)Go自身の`panic`を起こした場合、Weave側で捕まえる方法は無い**(15.6節・19.2節の例外処理の欠如と同じ理由)

### 19.8 モジュール(17節)

- **別々の場所にある2つのパッケージが同じディレクトリ名を持っている場合、両方を同じプログラムに取り込むと束縛名が衝突し、片方がもう片方を静かに上書きする(既知の未検出の不具合)。** コンパイルエラーにはならない(17.4節)。取り込むパッケージ同士のディレクトリ名が重ならないよう、開発者自身が注意する必要がある
- **パッケージ間の循環参照は禁止**(有向非巡回グラフでなければならない、17.5節)

### 19.9 標準ライブラリ・入出力

- **文字列操作の組み込み関数がほとんど無い。** `+`による結合と`string()`変換以外(部分文字列取得・分割・置換・トリム等)は無く、必要であればGo資産連携(15節)経由で`strings`パッケージ等を呼び出す
- **コマンドライン引数(12節)以外の実行環境とのやり取り(環境変数の取得、標準入力の読み取り)に専用の構文・組み込み関数は無い。** これらもGo資産連携経由(`os.Getenv`等)でのみ可能
